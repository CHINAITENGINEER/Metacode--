# 接口权限说明文档

## 📋 权限角色说明

- **管理员（admin）**：拥有所有权限
- **店员（staff）**：只能访问会员管理和积分记录（仅自己操作的记录）

---

## 🔐 接口权限明细表

### 一、认证接口（无需认证）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 | 公开 |
|---------|------|------|--------|------|------|
| `/auth/login` | POST | 管理员登录 | ✅ | ❌ | ✅ |
| `/auth/staff-login` | POST | 店员登录 | ❌ | ✅ | ✅ |

---

### 二、数据大屏模块（仅管理员）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/dashboard/stats` | GET | 获取核心指标统计 | ✅ | ❌ |
| `/dashboard/trends` | GET | 获取近30天趋势数据 | ✅ | ❌ |

**权限说明**：数据大屏包含全局统计数据，只有管理员可以访问。

---

### 三、会员管理模块（管理员 + 店员）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/members/list` | GET | 分页查询会员列表 | ✅ | ✅ |
| `/members/{id}` | GET | 获取会员详情 | ✅ | ✅ |
| `/members` | POST | 创建会员 | ✅ | ✅ |
| `/members/{id}/points/adjust` | POST | 调整会员积分 | ✅ | ✅ |

**权限说明**：管理员和店员都可以进行会员管理和积分操作。

---

### 四、积分记录模块（管理员 + 店员，数据权限不同）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/points-records/list` | GET | 分页查询积分记录 | ✅ | ✅ |

**权限说明**：
- **管理员**：可以查看所有会员的积分记录
- **店员**：只能查看自己操作的积分记录（在Service层已实现数据权限控制）

---

### 五、店员账号管理模块（仅管理员）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/staffs/list` | GET | 分页查询店员列表 | ✅ | ❌ |
| `/staffs/{id}` | GET | 获取店员详情 | ✅ | ❌ |
| `/staffs` | POST | 创建店员账号 | ✅ | ❌ |
| `/staffs/{id}/reset-password` | POST | 重置密码 | ✅ | ❌ |
| `/staffs/{id}/toggle-status` | POST | 启用/禁用账号 | ✅ | ❌ |

**权限说明**：店员账号管理只有管理员可以操作。

---

### 六、导出功能模块（仅管理员）

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/export/points-records` | GET | 导出积分记录 | ✅ | ❌ |
| `/export/consumption-records` | GET | 导出真钱消费记录 | ✅ | ❌ |
| `/export/financial-report` | GET | 导出财务统计报表 | ✅ | ❌ |

**权限说明**：导出功能用于财务统计，只有管理员可以访问。

---

### 七、其他接口

| 接口路径 | 方法 | 说明 | 管理员 | 店员 |
|---------|------|------|--------|------|
| `/test/**` | ALL | 测试接口 | ✅ | ✅ |
| `/service-orders/**` | ALL | 上门服务订单（预留） | ✅ | ✅ |
| `/doc.html` | GET | API文档 | ✅ | ✅ |
| `/v3/api-docs/**` | GET | OpenAPI规范 | ✅ | ✅ |

---

## ✅ 权限控制实现状态

### 已实现的权限控制

1. ✅ **基础认证**：所有接口（除登录和文档）都需要JWT Token认证
2. ✅ **接口级权限**：使用 `@RequireRole` 注解控制接口访问权限
3. ✅ **角色验证**：通过拦截器验证用户角色（admin/staff）
4. ✅ **数据权限**：积分记录查询中，店员只能查看自己操作的记录（Service层实现）

### 权限控制实现方式

1. **@RequireRole 注解**：在Controller类或方法上标注允许的角色
2. **RoleCheckInterceptor**：拦截器自动检查用户角色
3. **WebMvcConfig**：注册拦截器，排除登录和文档接口

---

## 🔧 建议实现的权限控制

### 方案一：在Controller方法上添加角色检查（简单）

在每个Controller方法中添加角色验证：

```java
@GetMapping("/dashboard/stats")
public Result<DashboardStatsVO> getStats(HttpServletRequest request) {
    // 检查是否为管理员
    if (!userContext.isAdmin(request)) {
        throw new BusinessException("无权限访问");
    }
    // ... 业务逻辑
}
```

### 方案二：使用Spring Security的@PreAuthorize（推荐）

1. 启用方法级安全
2. 在Controller方法上添加注解

```java
@PreAuthorize("hasRole('ADMIN')")
@GetMapping("/dashboard/stats")
public Result<DashboardStatsVO> getStats() {
    // ... 业务逻辑
}
```

### 方案三：自定义JWT过滤器（最灵活）

创建JWT认证过滤器，在过滤器中验证角色并设置权限。

---

## 📊 权限矩阵总结

| 模块 | 接口数 | 管理员权限 | 店员权限 | 数据权限 |
|------|--------|-----------|---------|---------|
| 认证接口 | 2 | ✅ | ✅ | - |
| 数据大屏 | 2 | ✅ | ❌ | - |
| 会员管理 | 4 | ✅ | ✅ | 相同 |
| 积分记录 | 1 | ✅ | ✅ | 不同（店员仅自己） |
| 店员管理 | 5 | ✅ | ❌ | - |
| 导出功能 | 3 | ✅ | ❌ | - |
| **总计** | **17** | **17** | **7** | - |

---

## 🎯 快速参考

### 管理员可以访问的接口
- ✅ 所有接口（17个）

### 店员可以访问的接口
- ✅ 登录接口（1个）
- ✅ 会员管理（4个）
- ✅ 积分记录查询（1个，但只能看自己的）
- ✅ 测试接口（1个）
- **总计：7个接口**

---

## 📝 注意事项

1. **当前状态**：所有认证用户都可以访问所有接口，需要在代码层面添加角色验证
2. **数据权限**：积分记录的数据权限已在Service层实现，店员只能查看自己操作的记录
3. **建议**：尽快实现接口级权限控制，确保安全性
