# 密码哈希值更新说明

## 问题描述

登录接口返回 HTTP 200，但响应体中的 `code` 是 1000（业务异常），提示"用户名或密码错误"。这是因为数据库中的 BCrypt 密码哈希值不匹配。

## 解决方案

### 方法1：使用 SQL 更新脚本（推荐）

1. **执行更新脚本**：
   ```sql
   -- 在数据库中执行 update_password_hash.sql
   ```

2. **验证更新**：
   ```sql
   SELECT username, password_hash FROM admins WHERE username = 'admin';
   ```

### 方法2：使用 Java 工具生成新的哈希值

1. **运行密码生成工具**：
   ```bash
   cd backend/huakang-service
   mvn compile exec:java -Dexec.mainClass="com.huakang.service.utils.PasswordHashGenerator"
   ```

2. **复制生成的哈希值**，然后执行：
   ```sql
   UPDATE admins 
   SET password_hash = '生成的哈希值'
   WHERE username = 'admin';
   ```

### 方法3：使用已知正确的哈希值

如果上述方法都不行，可以使用以下已验证的 BCrypt 哈希值（密码：123456）：

```sql
UPDATE admins 
SET password_hash = '$2a$10$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW'
WHERE username = 'admin';
```

## 验证步骤

1. **更新数据库后，重启应用**
2. **在 Knife4j 中测试登录**：
   - 用户名：`admin`
   - 密码：`123456`
3. **如果仍然失败**：
   - 检查数据库中的 `password_hash` 字段是否正确更新
   - 确认应用已重启
   - 清除浏览器缓存或使用无痕模式

## 注意事项

- BCrypt 每次生成的哈希值都不同（因为盐值随机），但都能验证同一个密码
- 更新密码哈希值后，需要重启应用才能生效
- 如果多次登录失败，账号可能被锁定，需要重置 `login_fail_count` 和 `locked_until`
