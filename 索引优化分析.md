# 数据库索引优化分析文档

## 📊 索引优化总览

本文档分析所有SQL查询场景，识别需要添加的索引以提升查询性能。

---

## 一、会员表 (members) 索引分析

### 1.1 现有索引
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_openid (openid)
UNIQUE KEY uk_phone (phone)
KEY idx_phone (phone)              -- 冗余，UNIQUE已包含
KEY idx_created_at (created_at)
KEY idx_is_deleted (is_deleted)
KEY idx_total_points (total_points)
```

### 1.2 查询场景分析

#### 场景1：会员列表查询（后台管理）
```sql
-- 查询条件：昵称、手机号搜索 + 分页 + 排序
SELECT * FROM members 
WHERE is_deleted = 0 
  AND (nickname LIKE '%关键词%' OR phone LIKE '%关键词%')
ORDER BY created_at DESC
LIMIT 10 OFFSET 0;
```
**问题：** `nickname` 字段没有索引，LIKE查询无法使用索引  
**优化：** 
- ❌ `nickname` 不适合建索引（LIKE '%xxx%' 无法使用索引）
- ✅ 如果使用 `phone LIKE '138%'`（前缀匹配），可以优化
- ✅ 建议：使用全文索引（FULLTEXT）或ES搜索引擎

#### 场景2：按积分排序查询
```sql
-- 查询积分排行榜
SELECT * FROM members 
WHERE is_deleted = 0 
ORDER BY total_points DESC
LIMIT 100;
```
**现状：** ✅ 已有 `idx_total_points` 索引，可以使用

#### 场景3：按注册时间统计
```sql
-- 近30天新注册用户
SELECT DATE(created_at) as date, COUNT(*) as count 
FROM members 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
  AND is_deleted = 0
GROUP BY DATE(created_at)
ORDER BY date;
```
**现状：** ✅ 已有 `idx_created_at` 索引，可以使用

#### 场景4：复合条件查询（需要优化）
```sql
-- 查询未删除且最近登录的会员
SELECT * FROM members 
WHERE is_deleted = 0 
  AND last_login_at IS NOT NULL
ORDER BY last_login_at DESC
LIMIT 20;
```
**问题：** `last_login_at` 没有索引  
**优化：** ⚠️ **需要添加索引**
```sql
ALTER TABLE members ADD KEY idx_last_login_at (last_login_at);
```

### 1.3 会员表索引优化建议

**需要添加的索引：**
```sql
-- 1. 最后登录时间索引（用于活跃用户查询）
ALTER TABLE members ADD KEY idx_last_login_at (last_login_at);

-- 2. 复合索引：软删除 + 创建时间（用于列表查询）
ALTER TABLE members ADD KEY idx_deleted_created (is_deleted, created_at DESC);
```

**已优化的索引：**
- ✅ `idx_total_points` - 积分排序
- ✅ `idx_created_at` - 时间统计
- ✅ `idx_is_deleted` - 软删除筛选

---

## 二、商品表 (products) 索引分析

### 2.1 现有索引
```sql
PRIMARY KEY (id)
KEY idx_type_status_deleted (type, status, is_deleted)  -- 复合索引
KEY idx_category (category)
KEY idx_status (status)                                -- 冗余，复合索引已包含
KEY idx_sort_order (sort_order)
KEY idx_is_deleted (is_deleted)                        -- 冗余，复合索引已包含
```

### 2.2 查询场景分析

#### 场景1：商品列表查询（小程序端 - 最重要）
```sql
-- 全量商品列表
SELECT * FROM products 
WHERE type = 1 
  AND status = 1 
  AND is_deleted = 0
ORDER BY sort_order DESC, id DESC
LIMIT 20 OFFSET 0;
```
**现状：** ✅ 已有 `idx_type_status_deleted` 复合索引，可以使用  
**优化：** ⚠️ **需要优化排序索引**
```sql
-- 添加排序索引
ALTER TABLE products ADD KEY idx_type_status_sort (type, status, is_deleted, sort_order DESC, id DESC);
```

#### 场景2：按分类查询商品
```sql
-- 按分类筛选
SELECT * FROM products 
WHERE category = '电视' 
  AND type = 1 
  AND status = 1 
  AND is_deleted = 0
ORDER BY sort_order DESC;
```
**问题：** 需要同时使用 `category` 和 `type_status_deleted` 索引  
**优化：** ⚠️ **需要添加复合索引**
```sql
ALTER TABLE products ADD KEY idx_category_type_status (category, type, status, is_deleted, sort_order DESC);
```

#### 场景3：积分商品列表
```sql
-- 积分商品列表
SELECT * FROM products 
WHERE type = 2 
  AND status = 1 
  AND is_deleted = 0
ORDER BY points_price ASC, sort_order DESC;
```
**问题：** 按积分价格排序没有索引  
**优化：** ⚠️ **需要添加索引**
```sql
ALTER TABLE products ADD KEY idx_type_status_points (type, status, is_deleted, points_price ASC, sort_order DESC);
```

#### 场景4：商品名称搜索（后台管理）
```sql
-- 商品名称模糊搜索
SELECT * FROM products 
WHERE name LIKE '%关键词%' 
  AND is_deleted = 0
ORDER BY created_at DESC;
```
**问题：** `name` 字段没有索引，LIKE查询无法使用索引  
**优化：** 
- ❌ 普通索引无法优化 `LIKE '%xxx%'`
- ✅ 建议：使用全文索引（FULLTEXT）或ES搜索引擎
- ✅ 如果使用 `name LIKE '关键词%'`（前缀匹配），可以添加索引

### 2.3 商品表索引优化建议

**需要添加的索引：**
```sql
-- 1. 全量商品列表排序优化
ALTER TABLE products ADD KEY idx_type_status_sort (type, status, is_deleted, sort_order DESC, id DESC);

-- 2. 分类查询优化
ALTER TABLE products ADD KEY idx_category_type_status (category, type, status, is_deleted, sort_order DESC);

-- 3. 积分商品按价格排序
ALTER TABLE products ADD KEY idx_type_status_points (type, status, is_deleted, points_price ASC, sort_order DESC);

-- 4. 商品名称前缀搜索（如果使用前缀匹配）
ALTER TABLE products ADD KEY idx_name_prefix (name(20));  -- 前缀索引，只索引前20个字符
```

**可以删除的冗余索引：**
```sql
-- 删除冗余索引（复合索引已包含）
ALTER TABLE products DROP KEY idx_status;
ALTER TABLE products DROP KEY idx_is_deleted;
```

---

## 三、积分记录表 (points_records) 索引分析 ⚠️ 最重要

### 3.1 现有索引
```sql
PRIMARY KEY (id)
KEY idx_member_created (member_id, created_at DESC)      -- 复合索引
KEY idx_operator_created (operator_type, operator_id, created_at DESC)  -- 复合索引
KEY idx_created_at (created_at)
KEY idx_change_type (change_type)
KEY idx_business_no (business_no)
KEY idx_product_id (product_id)
```

### 3.2 查询场景分析

#### 场景1：会员积分记录查询（小程序端 - 高频）
```sql
-- 查询会员的积分记录
SELECT * FROM points_records 
WHERE member_id = 123
ORDER BY created_at DESC
LIMIT 20 OFFSET 0;
```
**现状：** ✅ 已有 `idx_member_created` 索引，完美匹配

#### 场景2：店员查询自己的操作记录
```sql
-- 店员查询自己操作的积分记录
SELECT * FROM points_records 
WHERE operator_type = 'staff' 
  AND operator_id = 456
ORDER BY created_at DESC
LIMIT 20 OFFSET 0;
```
**现状：** ✅ 已有 `idx_operator_created` 索引，完美匹配

#### 场景3：管理员查询积分记录（带筛选）
```sql
-- 按会员、操作人、时间范围筛选
SELECT * FROM points_records 
WHERE created_at >= '2025-01-01' 
  AND created_at <= '2025-01-31'
  AND (member_id = 123 OR operator_id = 456)
ORDER BY created_at DESC;
```
**现状：** ✅ 已有 `idx_created_at` 索引，可以使用

#### 场景4：按变动类型统计（数据大屏）
```sql
-- 按变动类型统计
SELECT change_type, SUM(points) as total_points
FROM points_records 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY change_type;
```
**现状：** ✅ 已有 `idx_change_type` 和 `idx_created_at` 索引

#### 场景5：按业务单号查询（对账）
```sql
-- 按业务单号查询
SELECT * FROM points_records 
WHERE business_no = 'ORDER20250101001';
```
**现状：** ✅ 已有 `idx_business_no` 索引

#### 场景6：积分对账查询（存储过程）
```sql
-- 查询每个会员的最后一条记录
SELECT member_id, MAX(created_at) as last_time, MAX(balance_after) as last_balance
FROM points_records 
GROUP BY member_id;
```
**问题：** 需要按 `member_id` 分组，但需要取 `MAX(created_at)`  
**优化：** ✅ 已有 `idx_member_created` 索引，可以使用

#### 场景7：按商品统计积分兑换（新增需求）
```sql
-- 统计某个商品的积分兑换次数
SELECT product_id, COUNT(*) as exchange_count, SUM(ABS(points)) as total_points
FROM points_records 
WHERE change_type = '积分兑换' 
  AND product_id IS NOT NULL
GROUP BY product_id;
```
**现状：** ✅ 已有 `idx_product_id` 和 `idx_change_type` 索引

#### 场景8：复合条件查询（需要优化）
```sql
-- 查询某个会员在某个时间段的特定类型记录
SELECT * FROM points_records 
WHERE member_id = 123
  AND change_type = '消费赠送'
  AND created_at >= '2025-01-01'
ORDER BY created_at DESC;
```
**问题：** 需要同时使用 `member_id`、`change_type`、`created_at`  
**优化：** ⚠️ **需要添加复合索引**
```sql
ALTER TABLE points_records ADD KEY idx_member_type_created (member_id, change_type, created_at DESC);
```

### 3.3 积分记录表索引优化建议

**需要添加的索引：**
```sql
-- 1. 会员+变动类型+时间复合索引（用于筛选查询）
ALTER TABLE points_records ADD KEY idx_member_type_created (member_id, change_type, created_at DESC);

-- 2. 时间范围+变动类型统计（用于数据大屏）
ALTER TABLE points_records ADD KEY idx_created_type (created_at, change_type);
```

**已优化的索引：**
- ✅ `idx_member_created` - 会员积分历史（最重要）
- ✅ `idx_operator_created` - 店员查询自己的操作（最重要）
- ✅ `idx_created_at` - 时间范围查询
- ✅ `idx_change_type` - 变动类型统计
- ✅ `idx_business_no` - 业务单号查询
- ✅ `idx_product_id` - 商品关联查询

---

## 四、店员表 (staffs) 索引分析

### 4.1 现有索引
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_username (username)
KEY idx_status (status)
KEY idx_is_deleted (is_deleted)
KEY idx_created_at (created_at)
```

### 4.2 查询场景分析

#### 场景1：店员列表查询
```sql
-- 查询启用的店员列表
SELECT * FROM staffs 
WHERE status = 1 
  AND is_deleted = 0
ORDER BY created_at DESC;
```
**问题：** 需要同时使用 `status` 和 `is_deleted`  
**优化：** ⚠️ **需要添加复合索引**
```sql
ALTER TABLE staffs ADD KEY idx_status_deleted (status, is_deleted, created_at DESC);
```

### 4.3 店员表索引优化建议

**需要添加的索引：**
```sql
-- 复合索引：状态+软删除+创建时间
ALTER TABLE staffs ADD KEY idx_status_deleted (status, is_deleted, created_at DESC);
```

---

## 五、管理员表 (admins) 索引分析

### 5.1 现有索引
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_username (username)
KEY idx_is_deleted (is_deleted)
KEY idx_created_at (created_at)
```

### 5.2 查询场景分析

管理员表查询较少，现有索引已足够。

---

## 六、系统配置表 (system_configs) 索引分析

### 6.1 现有索引
```sql
PRIMARY KEY (id)
UNIQUE KEY uk_config_key (config_key)
KEY idx_config_group (config_group)
```

### 6.2 查询场景分析

#### 场景1：按分组查询配置
```sql
-- 查询某个分组的配置
SELECT * FROM system_configs 
WHERE config_group = 'wechat';
```
**现状：** ✅ 已有 `idx_config_group` 索引

配置表查询简单，现有索引已足够。

---

## 七、统计查询优化

### 7.1 数据大屏统计查询

#### 查询1：会员总数
```sql
SELECT COUNT(*) FROM members WHERE is_deleted = 0;
```
**优化：** ✅ 使用 `idx_is_deleted` 索引

#### 查询2：累计发放积分
```sql
SELECT SUM(points) FROM points_records WHERE points > 0;
```
**问题：** 全表扫描，数据量大时慢  
**优化：** ⚠️ **需要添加索引**
```sql
-- 添加 points 字段索引（用于统计）
ALTER TABLE points_records ADD KEY idx_points (points);
```

#### 查询3：近30天新用户趋势
```sql
SELECT DATE(created_at) as date, COUNT(*) as count 
FROM members 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
  AND is_deleted = 0
GROUP BY DATE(created_at)
ORDER BY date;
```
**优化：** ✅ 使用 `idx_created_at` 和 `idx_is_deleted` 索引  
**进一步优化：** ⚠️ **添加复合索引**
```sql
ALTER TABLE members ADD KEY idx_deleted_created (is_deleted, created_at);
```

#### 查询4：近30天积分发放/消耗趋势
```sql
SELECT DATE(created_at) as date, 
       SUM(CASE WHEN points > 0 THEN points ELSE 0 END) as issued,
       SUM(CASE WHEN points < 0 THEN ABS(points) ELSE 0 END) as consumed
FROM points_records 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY DATE(created_at)
ORDER BY date;
```
**优化：** ✅ 使用 `idx_created_at` 索引

---

## 八、索引优化SQL脚本

### 8.1 需要添加的索引（汇总）

```sql
-- ============================================
-- 索引优化脚本
-- 执行前请先备份数据库
-- ============================================

USE hk_electronics;

-- 1. 会员表索引优化
ALTER TABLE members ADD KEY idx_last_login_at (last_login_at);
ALTER TABLE members ADD KEY idx_deleted_created (is_deleted, created_at DESC);

-- 2. 商品表索引优化
-- 删除冗余索引
ALTER TABLE products DROP KEY idx_status;
ALTER TABLE products DROP KEY idx_is_deleted;

-- 添加优化索引
ALTER TABLE products ADD KEY idx_type_status_sort (type, status, is_deleted, sort_order DESC, id DESC);
ALTER TABLE products ADD KEY idx_category_type_status (category, type, status, is_deleted, sort_order DESC);
ALTER TABLE products ADD KEY idx_type_status_points (type, status, is_deleted, points_price ASC, sort_order DESC);

-- 3. 积分记录表索引优化
ALTER TABLE points_records ADD KEY idx_member_type_created (member_id, change_type, created_at DESC);
ALTER TABLE points_records ADD KEY idx_created_type (created_at, change_type);
ALTER TABLE points_records ADD KEY idx_points (points);  -- 用于统计查询

-- 4. 店员表索引优化
ALTER TABLE staffs ADD KEY idx_status_deleted (status, is_deleted, created_at DESC);

-- 5. 管理员表索引优化（可选）
ALTER TABLE admins ADD KEY idx_deleted_created (is_deleted, created_at DESC);
```

### 8.2 索引使用说明

**索引命名规范：**
- `idx_` 前缀：普通索引
- `uk_` 前缀：唯一索引
- `fk_` 前缀：外键索引

**索引选择原则：**
1. **高频查询字段**必须建索引
2. **WHERE条件字段**建索引
3. **ORDER BY字段**建索引（特别是DESC排序）
4. **复合索引**遵循最左前缀原则
5. **避免过度索引**（影响写入性能）

---

## 九、性能测试建议

### 9.1 测试方法

```sql
-- 1. 查看执行计划
EXPLAIN SELECT * FROM products WHERE type = 1 AND status = 1 ORDER BY sort_order DESC;

-- 2. 查看索引使用情况
SHOW INDEX FROM products;

-- 3. 分析慢查询
-- 开启慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;  -- 超过1秒的查询记录
```

### 9.2 性能目标

- **单表查询：** < 100ms
- **JOIN查询：** < 500ms
- **统计查询：** < 1s（可接受）
- **索引命中率：** > 95%

---

## 十、索引维护建议

### 10.1 定期维护

```sql
-- 1. 分析表（更新索引统计信息）
ANALYZE TABLE members, products, points_records;

-- 2. 优化表（整理碎片）
OPTIMIZE TABLE members, products, points_records;

-- 3. 检查未使用的索引
-- 通过慢查询日志分析，删除未使用的索引
```

### 10.2 监控指标

- **索引使用率：** 监控哪些索引未被使用
- **查询性能：** 监控慢查询
- **索引大小：** 监控索引占用的磁盘空间

---

## 📊 索引优化总结

### ✅ 必须添加的索引（高优先级）

1. **会员表：**
   - `idx_last_login_at` - 活跃用户查询
   - `idx_deleted_created` - 列表查询优化

2. **商品表：**
   - `idx_type_status_sort` - 商品列表排序优化
   - `idx_category_type_status` - 分类查询优化
   - `idx_type_status_points` - 积分商品价格排序

3. **积分记录表：**
   - `idx_member_type_created` - 会员筛选查询
   - `idx_created_type` - 统计查询优化
   - `idx_points` - 积分统计优化

4. **店员表：**
   - `idx_status_deleted` - 列表查询优化

### ⚠️ 注意事项

1. **索引不是越多越好：** 每个索引都会影响INSERT/UPDATE性能
2. **复合索引顺序：** 遵循最左前缀原则
3. **LIKE查询：** `LIKE '%xxx%'` 无法使用索引，建议使用全文索引或ES
4. **定期维护：** 定期分析表，优化索引

---

**执行索引优化脚本前，请先备份数据库！**
