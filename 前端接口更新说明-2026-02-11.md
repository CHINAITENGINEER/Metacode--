# 前端接口更新说明

**更新日期：** 2026-02-11  
**更新人：** 后端开发团队

---

## 📋 本次更新内容

### 1. 新增接口：查询下架商品列表

#### 接口信息
- **接口路径：** `GET /products/offline`
- **权限要求：** 仅管理员
- **功能说明：** 分页查询已下架的商品列表

#### 请求参数
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页大小，默认10 |
| type | Integer | 否 | 商品类型：1=全量商品，2=积分商品 |
| category | String | 否 | 商品分类 |
| name | String | 否 | 商品名称（模糊查询） |

#### 响应示例
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 50,
    "pages": 5,
    "current": 1,
    "size": 10,
    "records": [
      {
        "id": 1,
        "name": "海尔冰箱",
        "image": "https://example.com/product.jpg",
        "category": "家用电器",
        "type": 1,
        "price": 2999.00,
        "status": 0,
        "sortOrder": 100,
        "createdAt": "2025-01-01T10:00:00"
      }
    ]
  }
}
```

---

### 2. 修改接口：积分记录列表新增商品信息

#### 接口信息
- **接口路径：** `GET /points-records/list`
- **权限要求：** 管理员、店员
- **功能说明：** 查询积分记录时，新增返回关联的商品信息

#### 响应变更（新增字段）
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "memberId": 100,
        "memberNickname": "张三",
        "memberPhone": "13800138000",
        "changeType": "积分兑换",
        "points": -500,
        "balanceBefore": 1000,
        "balanceAfter": 500,
        "operatorType": "staff",
        "operatorName": "李四",
        "createdAt": "2025-01-01T10:00:00",
        "remark": "兑换商品",
        "productId": 10,           // ✨ 新增：关联商品ID
        "productName": "海尔冰箱"   // ✨ 新增：关联商品名称
      }
    ]
  }
}
```

#### 字段说明
| 字段名 | 类型 | 说明 |
|--------|------|------|
| productId | Long | 关联商品ID（如果该记录关联了商品） |
| productName | String | 关联商品名称（如果该记录关联了商品） |

**注意：** 如果积分记录未关联商品（如后台调整、新会员注册等），这两个字段为 `null`

---

### 3. 修改接口：导出积分记录新增商品列

#### 接口信息
- **接口路径：** `GET /export/points-records`
- **权限要求：** 仅管理员
- **功能说明：** 导出Excel时新增"关联商品"列

#### Excel列变更
| 列序号 | 列名 | 说明 |
|--------|------|------|
| 0 | 记录ID | 不变 |
| 1 | 会员昵称 | 不变 |
| 2 | 会员手机号 | 不变 |
| 3 | 变动类型 | 不变 |
| 4 | 变动分值 | 不变 |
| 5 | 变动前余额 | 不变 |
| 6 | 变动后余额 | 不变 |
| 7 | 操作人类型 | 不变 |
| 8 | 操作人姓名 | 不变 |
| 9 | 业务单号 | 不变 |
| 10 | 备注 | 不变 |
| **11** | **关联商品** | **✨ 新增列** |
| 12 | 操作时间 | 列序号调整（原11） |

#### 导出示例
```
记录ID | 会员昵称 | 会员手机号 | 变动类型 | 变动分值 | ... | 关联商品 | 操作时间
1      | 张三     | 13800138000 | 积分兑换 | -500    | ... | 海尔冰箱 | 2025-01-01 10:00:00
2      | 李四     | 13900139000 | 消费赠送 | 100     | ... |          | 2025-01-02 11:00:00
```

---

## ✅ 已实现功能对比

### 会议需求 vs 实现状态

| 需求 | 状态 | 说明 |
|------|------|------|
| 导出功能增加"关联电器"列 | ✅ 已完成 | 已在导出Excel中新增"关联商品"列 |
| 会员积分记录列表增加"关联电器"列 | ✅ 已完成 | 已在接口返回中新增 productId 和 productName 字段 |
| 商品上架支持图片上传 | ✅ 已支持 | 已有 `/products/upload` 接口 |
| 商品支持排序调整 | ✅ 已支持 | 商品实体已有 sortOrder 字段，可通过更新接口修改 |
| 查询下架商品 | ✅ 已完成 | 新增 `/products/offline` 接口 |

---

## ⚠️ 待实现功能（需前后端配合）

### 1. 商品视频上传功能
**状态：** ❌ 未实现  
**说明：** 当前商品表只支持图片（image、detailImages），不支持视频字段

**需要的改动：**
- 数据库：`products` 表需新增 `video_url` 字段（VARCHAR(500)）
- 后端：ProductVO、CreateProductDTO、UpdateProductDTO 需新增 videoUrl 字段
- 后端：OssService 需支持视频文件上传（当前只支持图片）
- 前端：上传组件需支持视频格式

**建议方案：**
```sql
-- 数据库修改
ALTER TABLE products ADD COLUMN video_url VARCHAR(500) COMMENT '商品视频URL' AFTER detail_images;
```

### 2. 商品拖拽排序接口
**状态：** ⚠️ 部分支持  
**说明：** 
- 当前可通过 `PUT /products/{id}` 接口修改单个商品的 sortOrder
- 但没有批量更新排序的接口

**建议新增接口：**
```
POST /products/batch-sort
功能：批量更新商品排序
请求体：
[
  { "id": 1, "sortOrder": 100 },
  { "id": 2, "sortOrder": 99 },
  { "id": 3, "sortOrder": 98 }
]
```

### 3. 导出文件命名交互
**状态：** ⚠️ 前端实现  
**说明：** 
- 当前后端已生成默认文件名（如：积分记录_20250101_20250131.xlsx）
- 文件命名弹窗需要前端实现，后端无需改动
- 前端可在下载后通过浏览器 API 修改文件名

---

## 🔧 前端开发建议

### 1. 积分记录列表展示商品信息
```javascript
// 在表格列配置中新增一列
{
  title: '关联电器',
  dataIndex: 'productName',
  key: 'productName',
  render: (text) => text || '-'  // 如果没有关联商品显示 -
}
```

### 2. 导出文件命名
```javascript
// 方案1：下载前弹窗让用户输入文件名
const handleExport = async () => {
  const fileName = await showFileNameDialog(); // 弹窗获取文件名
  const blob = await exportAPI.exportPointsRecords(params);
  downloadFile(blob, fileName || '积分记录.xlsx');
};

// 方案2：使用默认文件名（后端已生成）
const handleExport = async () => {
  const response = await exportAPI.exportPointsRecords(params);
  // 从响应头 Content-Disposition 中获取文件名
  const fileName = getFileNameFromHeader(response.headers);
  downloadFile(response.data, fileName);
};
```

### 3. 商品排序（拖拽）
```javascript
// 使用拖拽库（如 react-beautiful-dnd）
const handleDragEnd = async (result) => {
  const newList = reorder(productList, result.source.index, result.destination.index);
  
  // 批量更新排序（需要后端新增接口）
  const sortData = newList.map((item, index) => ({
    id: item.id,
    sortOrder: newList.length - index  // 倒序，数值越大越靠前
  }));
  
  await productAPI.batchUpdateSort(sortData);
};
```

---

## 📞 联系方式

如有疑问，请联系后端开发团队。

**注意事项：**
1. 所有接口都需要在请求头中携带 JWT Token
2. 权限不足时会返回 403 错误
3. 商品视频上传功能需要后续迭代实现
