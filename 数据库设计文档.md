# 华康电器连锁积分小程序 - 数据库设计文档

## 1. 数据库表结构设计

### 1.1 会员表 (members)
存储小程序用户/会员信息

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| openid | VARCHAR(64) | 微信OpenID | UNIQUE, NOT NULL |
| nickname | VARCHAR(100) | 微信昵称 | |
| avatar | VARCHAR(500) | 微信头像URL | |
| phone | VARCHAR(20) | 手机号 | INDEX |
| total_points | INT | 当前积分总额 | DEFAULT 0, NOT NULL |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY (openid)
- INDEX (phone)

---

### 1.2 商品表 (products)
存储全量商品和积分商品

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| name | VARCHAR(200) | 商品名称 | NOT NULL |
| image | VARCHAR(500) | 商品图片URL | |
| category | VARCHAR(50) | 商品分类 | INDEX |
| type | TINYINT | 商品类型：1=全量商品，2=积分商品 | NOT NULL, DEFAULT 1 |
| price | DECIMAL(10,2) | 商品价格（元）| |
| points_price | INT | 积分价格 | |
| status | TINYINT | 状态：1=上架，0=下架 | DEFAULT 1, INDEX |
| sort_order | INT | 排序权重 | DEFAULT 0 |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**索引：**
- PRIMARY KEY (id)
- INDEX (type, status)
- INDEX (category)

**说明：**
- type=1（全量商品）：使用 price 字段，points_price 为空
- type=2（积分商品）：使用 points_price 字段，price 可为空

---

### 1.3 积分记录表 (points_records)
记录所有积分变动历史

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| member_id | BIGINT | 会员ID | NOT NULL, INDEX, FOREIGN KEY |
| change_type | VARCHAR(50) | 变动类型 | NOT NULL, INDEX |
| points | INT | 变动分值（正数=增加，负数=扣除）| NOT NULL |
| operator_type | VARCHAR(20) | 操作人类型：admin/staff/system | NOT NULL |
| operator_id | BIGINT | 操作人ID（管理员或店员ID）| |
| operator_name | VARCHAR(100) | 操作人姓名（冗余字段，便于查询）| |
| remark | VARCHAR(500) | 备注/原因 | |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP, INDEX |

**索引：**
- PRIMARY KEY (id)
- INDEX (member_id)
- INDEX (operator_type, operator_id)
- INDEX (created_at)
- FOREIGN KEY (member_id) REFERENCES members(id)

**变动类型 (change_type) 枚举：**
- `消费赠送` - 消费后赠送积分
- `积分兑换` - 使用积分兑换商品
- `后台调整` - 管理员/店员手动调整
- `新会员注册` - 注册时赠送积分
- `积分扣除` - 其他扣除原因

---

### 1.4 店员表 (staffs)
存储店员账号信息

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| username | VARCHAR(50) | 登录账号 | UNIQUE, NOT NULL |
| password_hash | VARCHAR(255) | 密码哈希值 | NOT NULL |
| name | VARCHAR(100) | 店员姓名 | NOT NULL |
| status | TINYINT | 状态：1=启用，0=禁用 | DEFAULT 1, INDEX |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY (username)
- INDEX (status)

---

### 1.5 管理员表 (admins)
存储管理员账号信息

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| username | VARCHAR(50) | 登录账号 | UNIQUE, NOT NULL |
| password_hash | VARCHAR(255) | 密码哈希值 | NOT NULL |
| name | VARCHAR(100) | 管理员姓名 | NOT NULL |
| role | VARCHAR(20) | 角色（预留）| DEFAULT 'admin' |
| created_at | DATETIME | 创建时间 | DEFAULT CURRENT_TIMESTAMP |
| updated_at | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY (username)

---

### 1.6 系统配置表 (system_configs)
存储系统配置信息（如企业微信二维码）

| 字段名 | 类型 | 说明 | 约束 |
|--------|------|------|------|
| id | BIGINT | 主键ID | PRIMARY KEY, AUTO_INCREMENT |
| config_key | VARCHAR(100) | 配置键 | UNIQUE, NOT NULL |
| config_value | TEXT | 配置值 | |
| description | VARCHAR(500) | 配置说明 | |
| updated_at | DATETIME | 更新时间 | DEFAULT CURRENT_TIMESTAMP ON UPDATE |

**索引：**
- PRIMARY KEY (id)
- UNIQUE KEY (config_key)

**预置配置项：**
- `wechat_qrcode` - 企业微信咨询二维码URL

---

## 2. 数据库关系图

```
members (会员表)
  ├── id (PK)
  └── total_points (当前积分)

products (商品表)
  ├── id (PK)
  ├── type (1=全量商品, 2=积分商品)
  └── status (上架/下架)

points_records (积分记录表)
  ├── id (PK)
  ├── member_id (FK -> members.id)
  ├── operator_type (admin/staff/system)
  └── operator_id (管理员或店员ID)

staffs (店员表)
  ├── id (PK)
  └── username (登录账号)

admins (管理员表)
  ├── id (PK)
  └── username (登录账号)

system_configs (系统配置表)
  ├── id (PK)
  └── config_key (配置键)
```

---

## 3. 数据统计查询说明

### 3.1 数据大屏统计指标

**会员总数：**
```sql
SELECT COUNT(*) FROM members;
```

**累计发放积分总额：**
```sql
SELECT SUM(points) FROM points_records WHERE points > 0;
```

**累计消耗积分总额：**
```sql
SELECT ABS(SUM(points)) FROM points_records WHERE points < 0;
```

**当前积分池总额（所有会员积分总和）：**
```sql
SELECT SUM(total_points) FROM members;
```

**近30天新注册用户趋势：**
```sql
SELECT DATE(created_at) as date, COUNT(*) as count 
FROM members 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY DATE(created_at)
ORDER BY date;
```

**近30天积分发放/消耗趋势：**
```sql
SELECT DATE(created_at) as date, 
       SUM(CASE WHEN points > 0 THEN points ELSE 0 END) as issued,
       SUM(CASE WHEN points < 0 THEN ABS(points) ELSE 0 END) as consumed
FROM points_records 
WHERE created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY DATE(created_at)
ORDER BY date;
```

---

## 4. 数据库初始化建议

### 4.1 创建数据库
```sql
CREATE DATABASE hk_electronics CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4.2 初始化系统配置
```sql
INSERT INTO system_configs (config_key, config_value, description) 
VALUES ('wechat_qrcode', '', '企业微信咨询二维码URL');
```

### 4.3 创建默认管理员账号（密码需加密后存储）
```sql
-- 示例：密码为 admin123，需使用BCrypt等加密后存储
INSERT INTO admins (username, password_hash, name) 
VALUES ('admin', '$2b$10$...', '系统管理员');
```
