# 华康电器连锁积分小程序 - 系统架构设计文档

## 1. 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层                                 │
├──────────────────────┬──────────────────────────────────────┤
│   微信小程序 (C端)    │      后台管理Web (B端)                │
│  - 全量商品展示       │  - 管理员端                           │
│  - 积分商城          │  - 店员端                             │
│  - 个人中心          │                                       │
└──────────┬───────────┴──────────────┬───────────────────────┘
           │                           │
           │  HTTPS/API                │  HTTPS/API
           │                           │
┌──────────▼───────────────────────────▼───────────────────────┐
│                    API网关/负载均衡                            │
│              (可选：Nginx / 云服务商负载均衡)                  │
└──────────────────────────┬────────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────────┐
│                    应用服务器层                                │
│  ┌────────────────────────────────────────────────────────┐  │
│  │           后端API服务 (Node.js/Python/Java)            │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │  │
│  │  │  小程序API   │  │  后台管理API  │  │  认证授权   │ │  │
│  │  │  模块        │  │  模块         │  │  模块       │ │  │
│  │  └──────────────┘  └──────────────┘  └─────────────┘ │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐ │  │
│  │  │  商品管理    │  │  会员管理    │  │  积分管理   │ │  │
│  │  │  模块        │  │  模块         │  │  模块       │ │  │
│  │  └──────────────┘  └──────────────┘  └─────────────┘ │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────┬────────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────────┐
│                      数据存储层                                │
│  ┌──────────────────┐  ┌──────────────────┐                 │
│  │   MySQL数据库    │  │   文件存储/OSS    │                 │
│  │  - 业务数据      │  │  - 商品图片       │                 │
│  │  - 用户数据      │  │  - 二维码图片     │                 │
│  └──────────────────┘  └──────────────────┘                 │
└───────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────▼────────────────────────────────────┐
│                      第三方服务                                │
│  ┌──────────────────┐  ┌──────────────────┐                 │
│  │   微信小程序API   │  │   企业微信API     │                 │
│  │  - 用户授权      │  │  - 二维码生成     │                 │
│  │  - OpenID获取    │  │                  │                 │
│  └──────────────────┘  └──────────────────┘                 │
└───────────────────────────────────────────────────────────────┘
```

---

## 2. 技术栈推荐

### 2.1 前端技术栈

**微信小程序端：**
- 框架：原生小程序 / uni-app / Taro
- UI组件：Vant Weapp / ColorUI
- 状态管理：MobX / Vuex（如使用框架）
- 网络请求：wx.request 封装

**后台管理Web端：**
- 框架：Vue 3 + Vite / React + Vite
- UI组件库：Element Plus / Ant Design Vue
- 路由：Vue Router / React Router
- 状态管理：Pinia / Redux
- HTTP客户端：Axios

### 2.2 后端技术栈

**推荐方案一：Node.js (推荐，开发效率高)**
- 框架：Express / Koa / Nest.js
- ORM：Sequelize / TypeORM / Prisma
- 认证：JWT (jsonwebtoken)
- 密码加密：bcrypt
- 文件上传：multer + OSS

**推荐方案二：Python (适合数据分析)**
- 框架：Flask / FastAPI / Django
- ORM：SQLAlchemy / Django ORM
- 认证：JWT (PyJWT)
- 密码加密：bcrypt

**推荐方案三：Java (企业级，性能稳定)**
- 框架：Spring Boot
- ORM：MyBatis / JPA
- 认证：Spring Security + JWT

### 2.3 数据库
- MySQL 5.7+ / MySQL 8.0
- 字符集：utf8mb4
- 存储引擎：InnoDB

### 2.4 文件存储
- 阿里云OSS / 腾讯云COS / 华为云OBS
- 或：本地存储（不推荐生产环境）

### 2.5 部署与运维
- 服务器：阿里云ECS / 华为云ECS
- 反向代理：Nginx
- 进程管理：PM2 (Node.js) / Supervisor (Python) / systemd
- 监控：云监控 / 自建监控

---

## 3. 系统模块划分

### 3.1 小程序API模块

```
小程序API
├── 认证授权
│   ├── 微信登录授权
│   └── OpenID获取与会员创建
├── 商品模块
│   ├── 全量商品列表
│   ├── 积分商品列表
│   └── 商品详情
├── 会员模块
│   ├── 会员信息查询
│   ├── 积分总览
│   └── 积分记录查询
└── 系统模块
    └── 企业微信二维码获取
```

### 3.2 后台管理API模块

```
后台管理API
├── 认证授权
│   ├── 管理员登录
│   ├── 店员登录
│   └── JWT Token验证
├── 数据大屏模块（仅管理员）
│   ├── 会员总数统计
│   ├── 积分统计
│   └── 趋势图表数据
├── 商品管理模块（仅管理员）
│   ├── 全量商品CRUD
│   └── 积分商品CRUD
├── 会员管理模块
│   ├── 会员列表查询
│   ├── 会员创建
│   ├── 会员详情
│   └── 积分操作（增/删/改）
├── 积分记录模块
│   ├── 积分记录列表（管理员：全部，店员：仅自己）
│   └── 记录筛选查询
├── 店员账号管理模块（仅管理员）
│   ├── 店员账号CRUD
│   ├── 启用/禁用
│   └── 密码重置
└── 系统设置模块（仅管理员）
    └── 企业微信二维码上传/更新
```

---

## 4. API接口设计

### 4.1 小程序端API

**基础URL：** `https://api.example.com/miniapp`

| 接口路径 | 方法 | 说明 | 权限 |
|---------|------|------|------|
| `/auth/wechat-login` | POST | 微信登录授权 | 公开 |
| `/products/list` | GET | 全量商品列表 | 公开 |
| `/products/points-list` | GET | 积分商品列表 | 公开 |
| `/member/info` | GET | 会员信息 | 需登录 |
| `/member/points` | GET | 积分总览 | 需登录 |
| `/member/points-records` | GET | 积分记录 | 需登录 |
| `/system/wechat-qrcode` | GET | 获取企业微信二维码 | 公开 |

### 4.2 后台管理API

**基础URL：** `https://api.example.com/admin`

| 接口路径 | 方法 | 说明 | 权限 |
|---------|------|------|------|
| `/auth/admin-login` | POST | 管理员登录 | 公开 |
| `/auth/staff-login` | POST | 店员登录 | 公开 |
| `/dashboard/stats` | GET | 数据大屏统计 | 管理员 |
| `/dashboard/trends` | GET | 趋势图表数据 | 管理员 |
| `/products/list` | GET | 商品列表 | 管理员 |
| `/products/create` | POST | 创建商品 | 管理员 |
| `/products/update` | PUT | 更新商品 | 管理员 |
| `/products/delete` | DELETE | 下架商品 | 管理员 |
| `/members/list` | GET | 会员列表 | 管理员/店员 |
| `/members/create` | POST | 创建会员 | 管理员/店员 |
| `/members/{id}` | GET | 会员详情 | 管理员/店员 |
| `/members/{id}/points/adjust` | POST | 调整积分 | 管理员/店员 |
| `/points-records/list` | GET | 积分记录列表 | 管理员/店员 |
| `/staffs/list` | GET | 店员列表 | 管理员 |
| `/staffs/create` | POST | 创建店员 | 管理员 |
| `/staffs/{id}/reset-password` | POST | 重置密码 | 管理员 |
| `/staffs/{id}/toggle-status` | POST | 启用/禁用 | 管理员 |
| `/system/config/wechat-qrcode` | GET | 获取二维码 | 管理员 |
| `/system/config/wechat-qrcode` | POST | 上传二维码 | 管理员 |

---

## 5. 安全设计

### 5.1 认证授权
- **小程序端：** 使用微信OpenID作为用户标识，JWT Token用于API调用
- **后台管理：** 账号密码登录，JWT Token认证
- **Token过期时间：** 建议2-4小时

### 5.2 权限控制
- **角色区分：** 管理员(admin) / 店员(staff)
- **接口权限：** 基于角色的访问控制(RBAC)
- **数据权限：** 店员只能查看自己操作的积分记录

### 5.3 数据安全
- 密码使用BCrypt加密存储
- API接口使用HTTPS
- 敏感操作记录日志
- SQL注入防护（使用ORM参数化查询）
- XSS防护（输入验证和输出转义）

---

## 6. 部署架构

### 6.1 开发环境
```
本地开发
├── 小程序开发工具
├── 本地后端服务 (localhost:3000)
└── 本地MySQL数据库
```

### 6.2 生产环境（推荐）

**方案一：单服务器部署（适合初期）**
```
云服务器 (2核4G)
├── Nginx (反向代理 + 静态资源)
├── Node.js/Python应用
├── MySQL数据库
└── 文件存储（本地或OSS）
```

**方案二：分离部署（推荐，更稳定）**
```
应用服务器 (2核4G)
├── Nginx
└── Node.js/Python应用

数据库服务器 (2核4G)
└── MySQL

文件存储
└── 阿里云OSS / 腾讯云COS
```

### 6.3 域名与SSL
- **API域名：** `api.example.com`（需ICP备案）
- **后台管理域名：** `admin.example.com`（需ICP备案）
- **SSL证书：** 使用免费Let's Encrypt或云服务商证书

---

## 7. 性能优化建议

### 7.1 数据库优化
- 合理使用索引
- 分页查询限制（避免全表扫描）
- 定期清理历史数据（可选）

### 7.2 缓存策略
- Redis缓存热点数据（商品列表、统计数据）
- 缓存过期时间：5-10分钟

### 7.3 图片优化
- 图片压缩和CDN加速
- 使用WebP格式（小程序支持）
- 懒加载

### 7.4 API优化
- 接口响应时间 < 500ms
- 使用分页加载
- 数据字段精简（不返回不必要字段）

---

## 8. 监控与日志

### 8.1 日志记录
- 访问日志（Nginx）
- 应用日志（错误、操作记录）
- 数据库慢查询日志

### 8.2 监控指标
- 服务器CPU、内存、磁盘
- 数据库连接数
- API响应时间
- 错误率

---

## 9. 开发流程建议

### 9.1 版本控制
- Git代码管理
- 分支策略：main（生产）、develop（开发）、feature/*（功能分支）

### 9.2 接口文档
- 使用Swagger / Postman生成API文档
- 前后端接口联调前确认

### 9.3 测试
- 单元测试（关键业务逻辑）
- 接口测试（Postman / Jest）
- 小程序真机测试

---

## 10. 项目目录结构示例（Node.js + Express）

```
backend/
├── config/                 # 配置文件
│   ├── database.js        # 数据库配置
│   └── config.js          # 应用配置
├── controllers/           # 控制器
│   ├── authController.js
│   ├── productController.js
│   ├── memberController.js
│   └── ...
├── models/                # 数据模型
│   ├── Member.js
│   ├── Product.js
│   ├── PointsRecord.js
│   └── ...
├── routes/                # 路由
│   ├── miniapp.js        # 小程序路由
│   ├── admin.js          # 后台管理路由
│   └── index.js
├── middlewares/           # 中间件
│   ├── auth.js           # 认证中间件
│   ├── errorHandler.js   # 错误处理
│   └── ...
├── services/              # 业务逻辑
│   ├── wechatService.js  # 微信服务
│   ├── pointsService.js  # 积分服务
│   └── ...
├── utils/                 # 工具函数
│   ├── jwt.js
│   ├── bcrypt.js
│   └── ...
├── uploads/               # 上传文件目录
├── logs/                  # 日志目录
├── app.js                 # 应用入口
└── server.js              # 服务器启动文件
```
