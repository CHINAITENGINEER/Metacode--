# 华康电器连锁积分小程序 - 企业级数据库设计说明

## 📋 建表逻辑梳理

### 一、核心设计原则

#### 1. **数据完整性原则**
- ✅ 所有外键关系必须明确，使用 FOREIGN KEY 约束
- ✅ 业务关键字段使用 CHECK 约束（如价格不能为负）
- ✅ 唯一性约束确保业务数据不重复（如手机号、OpenID）
- ✅ 非空约束保证核心字段完整性

#### 2. **审计追踪原则**
- ✅ 所有表必须有 `created_at`、`updated_at` 时间戳
- ✅ 核心业务表（会员、积分记录）需要软删除 `is_deleted`
- ✅ 积分记录表需要记录操作前后余额，便于对账审计
- ✅ 操作日志需要记录操作人、IP、设备信息

#### 3. **性能优化原则**
- ✅ 高频查询字段建立索引（如会员手机号、商品分类）
- ✅ 复合索引优化多条件查询（如 type+status）
- ✅ 积分记录表按时间分区（数据量大时）
- ✅ 避免过度索引（影响写入性能）

#### 4. **业务安全原则**
- ✅ 积分操作必须事务化，保证数据一致性
- ✅ 会员积分总额与积分记录流水必须一致（定期对账）
- ✅ 敏感操作（积分调整）需要审批流程（可选）
- ✅ 密码必须加密存储，使用 BCrypt

#### 5. **扩展性原则**
- ✅ 预留扩展字段（如商品表添加库存、详情等）
- ✅ 使用枚举值而非硬编码（变动类型、商品类型）
- ✅ 配置表设计支持动态配置

---

## 二、表结构设计详解

### 2.1 会员表 (members) - 核心业务表

**业务逻辑：**
- 会员通过微信OpenID唯一标识
- 手机号作为辅助查询条件（可补充，非必填）
- 积分总额需要与积分记录表保持一致（通过定时任务对账）

**关键设计点：**
```sql
-- 1. OpenID 必须唯一（微信用户唯一标识）
UNIQUE KEY `uk_openid` (`openid`)

-- 2. 手机号唯一性（业务要求：一个手机号只能绑定一个会员）
UNIQUE KEY `uk_phone` (`phone`) WHERE phone IS NOT NULL

-- 3. 积分字段使用 INT，范围 -2,147,483,648 到 2,147,483,647
--    对于5万用户规模，单个用户积分不会超过此范围

-- 4. 软删除设计（企业级必须）
`is_deleted` TINYINT DEFAULT 0 COMMENT '是否删除：0=否，1=是'

-- 5. 最后登录时间（用于用户活跃度分析）
`last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间'
```

**数据一致性保证：**
- 会员积分总额 = SUM(积分记录表中该会员的所有变动)
- 需要定时任务（每天凌晨）对账，发现不一致时告警

---

### 2.2 商品表 (products) - 商品管理

**业务逻辑：**
- 一张表存储两种商品类型（全量商品、积分商品）
- 通过 `type` 字段区分：1=全量商品，2=积分商品
- 全量商品使用 `price`，积分商品使用 `points_price`

**关键设计点：**
```sql
-- 1. 商品类型枚举
`type` TINYINT NOT NULL DEFAULT 1 
COMMENT '商品类型：1=全量商品，2=积分商品'
CHECK (type IN (1, 2))

-- 2. 价格约束（不能为负）
`price` DECIMAL(10,2) DEFAULT NULL 
CHECK (price IS NULL OR price >= 0)

`points_price` INT DEFAULT NULL 
CHECK (points_price IS NULL OR points_price > 0)

-- 3. 业务规则约束（通过应用层或触发器保证）
--    全量商品：price 必填，points_price 为空
--    积分商品：points_price 必填，price 可为空（显示原价）

-- 4. 软删除（商品下架不删除，保留历史数据）
`is_deleted` TINYINT DEFAULT 0

-- 5. 扩展字段（企业级预留）
`description` TEXT COMMENT '商品描述'
`detail_images` JSON COMMENT '详情图片（JSON数组）'
`stock` INT DEFAULT 0 COMMENT '库存（预留）'
```

**索引策略：**
- `(type, status, is_deleted)` 复合索引：查询上架商品列表
- `category` 索引：按分类筛选
- `sort_order` 索引：排序查询

---

### 2.3 积分记录表 (points_records) - 核心流水表 ⚠️

**业务逻辑：**
- **这是最重要的业务表**，记录所有积分变动，不可删除
- 每条记录必须包含：变动前余额、变动后余额（便于对账）
- 操作人信息必须完整（类型+ID+姓名）
- 支持按操作人筛选（店员只能看自己的操作）

**关键设计点：**
```sql
-- 1. 业务单号（便于对账、追溯）
`business_no` VARCHAR(64) DEFAULT NULL COMMENT '业务单号（如订单号、兑换单号）'
UNIQUE KEY `uk_business_no` (`business_no`) WHERE business_no IS NOT NULL

-- 2. 积分余额快照（审计必须）
`balance_before` INT NOT NULL COMMENT '变动前积分余额'
`balance_after` INT NOT NULL COMMENT '变动后积分余额'
CHECK (balance_after = balance_before + points)

-- 3. 操作人信息（冗余设计，便于查询）
`operator_type` VARCHAR(20) NOT NULL COMMENT '操作人类型：admin/staff/system'
`operator_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '操作人ID'
`operator_name` VARCHAR(100) DEFAULT NULL COMMENT '操作人姓名'

-- 4. 审计字段（企业级必须）
`ip_address` VARCHAR(50) DEFAULT NULL COMMENT '操作IP地址'
`user_agent` VARCHAR(500) DEFAULT NULL COMMENT '用户代理（设备信息）'

-- 5. 变动类型枚举（建议使用字典表，这里用VARCHAR）
`change_type` VARCHAR(50) NOT NULL COMMENT '变动类型'
-- 枚举值：消费赠送、积分兑换、后台调整、新会员注册、积分扣除、积分过期等

-- 6. 关联商品（如果是积分兑换）
`product_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联商品ID（积分兑换时）'
FOREIGN KEY (`product_id`) REFERENCES `products`(`id`)
```

**数据一致性保证：**
- 插入积分记录时，必须同时更新会员表的 `total_points`
- 使用数据库事务保证原子性
- 定期对账：`members.total_points` 应该等于该会员最后一条记录的 `balance_after`

**性能优化：**
- 按 `created_at` 分区（按月或按年，数据量大时）
- 索引：`(member_id, created_at)` 用于查询会员积分历史
- 索引：`(operator_type, operator_id, created_at)` 用于店员查询自己的操作

---

### 2.4 店员表 (staffs) 和 管理员表 (admins)

**业务逻辑：**
- 两个表结构相似，但权限不同
- 店员账号由管理员创建和管理
- 支持启用/禁用，不删除账号（保留操作记录）

**关键设计点：**
```sql
-- 1. 统一设计（两个表结构一致）
--    如果未来需要统一管理，可以合并为 users 表 + role 字段

-- 2. 安全字段
`password_hash` VARCHAR(255) NOT NULL COMMENT 'BCrypt加密后的密码'
`last_login_at` DATETIME DEFAULT NULL COMMENT '最后登录时间'
`last_login_ip` VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP'
`login_fail_count` TINYINT DEFAULT 0 COMMENT '连续登录失败次数'
`locked_until` DATETIME DEFAULT NULL COMMENT '账号锁定到期时间（防暴力破解）'

-- 3. 软删除
`is_deleted` TINYINT DEFAULT 0

-- 4. 扩展字段（企业级）
`email` VARCHAR(100) DEFAULT NULL COMMENT '邮箱'
`phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号'
`department` VARCHAR(50) DEFAULT NULL COMMENT '部门（预留）'
```

**安全策略：**
- 密码使用 BCrypt，cost=10
- 连续5次登录失败，锁定账号30分钟
- 记录登录日志（可单独建表）

---

### 2.5 系统配置表 (system_configs)

**业务逻辑：**
- 存储系统级配置（如企业微信二维码）
- 支持动态更新，无需重启服务

**关键设计点：**
```sql
-- 1. 配置分组（便于管理）
`config_group` VARCHAR(50) DEFAULT 'default' COMMENT '配置分组'
INDEX `idx_group` (`config_group`)

-- 2. 配置类型（字符串、数字、JSON等）
`config_type` VARCHAR(20) DEFAULT 'string' COMMENT '配置类型：string/number/json/boolean'

-- 3. 配置版本（可选，用于配置回滚）
`version` INT DEFAULT 1 COMMENT '配置版本号'
```

---

## 三、数据一致性保证机制

### 3.1 积分数据一致性

**问题：** 会员表的 `total_points` 必须与积分记录表一致

**解决方案：**

1. **应用层事务保证（推荐）**
```sql
BEGIN;
  -- 1. 插入积分记录
  INSERT INTO points_records (member_id, points, balance_before, balance_after, ...)
  VALUES (?, ?, ?, ?, ...);
  
  -- 2. 更新会员积分
  UPDATE members SET total_points = total_points + ? WHERE id = ?;
COMMIT;
```

2. **数据库触发器（备选，不推荐，影响性能）**
```sql
CREATE TRIGGER trg_points_records_after_insert
AFTER INSERT ON points_records
FOR EACH ROW
BEGIN
  UPDATE members 
  SET total_points = NEW.balance_after 
  WHERE id = NEW.member_id;
END;
```

3. **定时对账任务（必须）**
```sql
-- 每天凌晨执行，发现不一致时告警
SELECT 
  m.id,
  m.total_points AS db_points,
  COALESCE(MAX(pr.balance_after), 0) AS calculated_points
FROM members m
LEFT JOIN points_records pr ON m.id = pr.member_id
GROUP BY m.id
HAVING db_points != calculated_points;
```

---

### 3.2 商品数据一致性

**业务规则：**
- 全量商品：`price` 必填，`points_price` 为空
- 积分商品：`points_price` 必填，`price` 可为空

**实现方式：**
- 应用层校验（推荐）
- 数据库 CHECK 约束（MySQL 8.0.16+支持）

---

## 四、索引设计策略

### 4.1 会员表索引

```sql
PRIMARY KEY (id)                    -- 主键，自动创建
UNIQUE KEY uk_openid (openid)        -- 微信登录查询
UNIQUE KEY uk_phone (phone)          -- 手机号查询（WHERE phone IS NOT NULL）
INDEX idx_created_at (created_at)    -- 注册时间统计
INDEX idx_is_deleted (is_deleted)    -- 软删除筛选
```

### 4.2 商品表索引

```sql
PRIMARY KEY (id)
INDEX idx_type_status (type, status, is_deleted)  -- 商品列表查询（最重要）
INDEX idx_category (category)                     -- 分类筛选
INDEX idx_sort_order (sort_order)                 -- 排序
```

### 4.3 积分记录表索引

```sql
PRIMARY KEY (id)
INDEX idx_member_created (member_id, created_at DESC)  -- 会员积分历史（最重要）
INDEX idx_operator (operator_type, operator_id, created_at DESC)  -- 店员查询自己的操作
INDEX idx_created_at (created_at)                      -- 时间范围查询
INDEX idx_change_type (change_type)                    -- 按类型统计
INDEX idx_business_no (business_no)                   -- 业务单号查询
```

**注意：** 积分记录表数据量大，考虑按时间分区

---

## 五、扩展性设计

### 5.1 预留字段

- **会员表：** 可添加 `level`（会员等级）、`birthday`（生日）、`address`（地址）等
- **商品表：** 可添加 `brand`（品牌）、`specs`（规格）、`warranty`（保修期）等
- **积分记录表：** 可添加 `expire_at`（积分过期时间）等

### 5.2 字典表设计（可选）

如果变动类型、商品类型等枚举值较多，建议使用字典表：

```sql
CREATE TABLE `dict_items` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `dict_type` VARCHAR(50) NOT NULL COMMENT '字典类型',
  `dict_code` VARCHAR(50) NOT NULL COMMENT '字典编码',
  `dict_value` VARCHAR(200) NOT NULL COMMENT '字典值',
  `sort_order` INT DEFAULT 0,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_type_code` (`dict_type`, `dict_code`)
) COMMENT='字典表';
```

---

## 六、企业级优化建议

### 6.1 数据归档策略

- **积分记录表：** 超过1年的数据归档到历史表
- **商品表：** 下架超过1年的商品可归档

### 6.2 读写分离（数据量大时）

- 主库：写操作
- 从库：读操作（商品列表、积分记录查询）

### 6.3 缓存策略

- **Redis缓存：**
  - 商品列表（5分钟过期）
  - 会员信息（10分钟过期）
  - 系统配置（30分钟过期）
  - 数据大屏统计（1分钟过期）

### 6.4 监控告警

- 积分数据不一致告警
- 数据库慢查询告警
- 积分记录表数据量告警（超过100万条）

---

## 七、建表顺序建议

1. **基础表（无外键依赖）**
   - `admins` 管理员表
   - `staffs` 店员表
   - `system_configs` 系统配置表

2. **业务核心表**
   - `members` 会员表
   - `products` 商品表

3. **关联表（有外键依赖）**
   - `points_records` 积分记录表（依赖 members、products）

---

## 八、总结

### ✅ 企业级设计要点

1. **数据完整性：** 外键约束、CHECK约束、唯一性约束
2. **审计追踪：** 时间戳、操作人、IP地址、余额快照
3. **数据一致性：** 事务保证、定时对账
4. **性能优化：** 合理索引、分区策略、缓存
5. **安全设计：** 密码加密、登录锁定、软删除
6. **扩展性：** 预留字段、字典表、配置表

### ⚠️ 特别注意

1. **积分记录表是核心**，设计必须严谨，不可删除
2. **积分总额一致性**必须通过事务+对账双重保证
3. **店员权限**通过 `operator_type` + `operator_id` 实现数据隔离
4. **软删除**保证历史数据可追溯
