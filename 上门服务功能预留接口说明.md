# 上门服务功能预留接口说明

## 📋 功能概述

为未来可能实现的"呼叫师傅上门服务"功能预留接口，现阶段不实现具体逻辑，仅提供接口定义，方便二次开发时扩展。

---

## 🎯 预留功能场景

### 业务场景
- 会员购买电器后，需要师傅上门安装
- 会员需要维修、清洗、保养等服务
- 通过小程序预约师傅上门服务

### 功能模块
1. **订单管理**
   - 创建服务订单
   - 查询订单列表
   - 订单状态管理（待接单、已接单、进行中、已完成、已取消）

2. **师傅管理**（未来可能需要）
   - 师傅信息管理
   - 师傅接单
   - 师傅评价

3. **订单评价**
   - 服务完成后评价
   - 评价展示

---

## 📡 预留接口列表

### 后台管理端接口（huakang-admin）

#### 1. 创建上门服务订单
```
POST /api/admin/service-orders
```

**请求参数（预留）：**
```json
{
  "memberId": 123,
  "serviceType": "安装",
  "address": "广东省东莞市XX街道XX号",
  "detailAddress": "详细地址",
  "contactPhone": "13800138000",
  "contactName": "王女士",
  "serviceTime": "2025-02-01 14:00:00",
  "remark": "需要安装空调",
  "productId": 456
}
```

**当前返回：**
```json
{
  "code": 500,
  "message": "此功能为预留接口，暂未实现。如需使用，请联系开发团队进行二次开发。",
  "data": null
}
```

---

#### 2. 查询服务订单列表
```
GET /api/admin/service-orders
```

**查询参数：**
- `memberId` (可选) - 会员ID
- `status` (可选) - 订单状态
- `startTime` (可选) - 开始时间
- `endTime` (可选) - 结束时间
- `page` (默认1) - 页码
- `size` (默认10) - 每页大小

**当前返回：** 提示信息

---

#### 3. 查询订单详情
```
GET /api/admin/service-orders/{id}
```

**当前返回：** 提示信息

---

#### 4. 更新订单状态
```
PUT /api/admin/service-orders/{id}/status?status=已完成
```

**当前返回：** 提示信息

---

#### 5. 师傅接单
```
POST /api/admin/service-orders/{id}/accept?masterId=789
```

**当前返回：** 提示信息

---

#### 6. 取消订单
```
POST /api/admin/service-orders/{id}/cancel
```

**当前返回：** 提示信息

---

### 小程序端接口（huakang-miniapp）

#### 1. 创建上门服务订单
```
POST /api/miniapp/service-orders
```

**当前返回：** 提示信息

---

#### 2. 查询我的服务订单
```
GET /api/miniapp/service-orders/my-orders?status=待接单&page=1&size=10
```

**当前返回：** 提示信息

---

#### 3. 查询订单详情
```
GET /api/miniapp/service-orders/{id}
```

**当前返回：** 提示信息

---

#### 4. 取消订单
```
POST /api/miniapp/service-orders/{id}/cancel
```

**当前返回：** 提示信息

---

#### 5. 订单评价
```
POST /api/miniapp/service-orders/{id}/evaluate?rating=5&comment=服务很好
```

**当前返回：** 提示信息

---

## 📊 数据库设计建议（未来扩展）

### 服务订单表（service_orders）

```sql
CREATE TABLE IF NOT EXISTS `service_orders` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `order_no` VARCHAR(64) NOT NULL COMMENT '订单号',
  `member_id` BIGINT UNSIGNED NOT NULL COMMENT '会员ID',
  `service_type` VARCHAR(50) NOT NULL COMMENT '服务类型：安装/维修/清洗/保养',
  `address` VARCHAR(200) NOT NULL COMMENT '服务地址',
  `detail_address` VARCHAR(500) DEFAULT NULL COMMENT '详细地址',
  `contact_phone` VARCHAR(20) NOT NULL COMMENT '联系电话',
  `contact_name` VARCHAR(100) DEFAULT NULL COMMENT '联系人姓名',
  `service_time` DATETIME NOT NULL COMMENT '预约服务时间',
  `status` VARCHAR(20) NOT NULL DEFAULT '待接单' COMMENT '订单状态：待接单/已接单/进行中/已完成/已取消',
  `master_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '师傅ID',
  `service_fee` DECIMAL(10,2) DEFAULT 0.00 COMMENT '服务费用（元）',
  `product_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联商品ID',
  `remark` VARCHAR(500) DEFAULT NULL COMMENT '备注',
  `rating` TINYINT DEFAULT NULL COMMENT '评价星级（1-5）',
  `comment` VARCHAR(500) DEFAULT NULL COMMENT '评价内容',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_member_id` (`member_id`),
  KEY `idx_status` (`status`),
  KEY `idx_master_id` (`master_id`),
  KEY `idx_service_time` (`service_time`),
  CONSTRAINT `fk_service_orders_member` FOREIGN KEY (`member_id`) REFERENCES `members` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='服务订单表';
```

### 师傅表（masters）- 可选

```sql
CREATE TABLE IF NOT EXISTS `masters` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `name` VARCHAR(100) NOT NULL COMMENT '师傅姓名',
  `phone` VARCHAR(20) NOT NULL COMMENT '联系电话',
  `service_types` VARCHAR(200) DEFAULT NULL COMMENT '服务类型（多个用逗号分隔）',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1=启用，0=禁用',
  `rating` DECIMAL(3,2) DEFAULT 0.00 COMMENT '平均评分',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='师傅表';
```

---

## 🔧 代码结构

### 已创建的预留代码

```
backend/
├── huakang-service/
│   ├── service/
│   │   └── ServiceOrderService.java          # 服务接口（预留，未实现）
│   └── dto/serviceorder/
│       ├── ServiceOrderDTO.java              # 订单DTO（预留）
│       └── ServiceOrderVO.java               # 订单VO（预留）
│
├── huakang-admin/
│   └── controller/
│       └── ServiceOrderController.java       # 后台管理接口（预留）
│
└── huakang-miniapp/
    └── controller/
        └── ServiceOrderController.java       # 小程序接口（预留）
```

---

## 🚀 二次开发指南

### 开发步骤

#### 1. 创建数据库表
执行上述SQL创建 `service_orders` 表（和可选的 `masters` 表）

#### 2. 创建实体类和Mapper
```java
// ServiceOrder.java - 实体类
// ServiceOrderMapper.java - Mapper接口
// Master.java - 师傅实体类（可选）
// MasterMapper.java - 师傅Mapper（可选）
```

#### 3. 实现Service
```java
@Service
public class ServiceOrderServiceImpl implements ServiceOrderService {
    // 实现接口中的方法
}
```

#### 4. 实现Controller
修改 `ServiceOrderController`，将预留的提示信息替换为实际业务逻辑

#### 5. 添加业务逻辑
- 订单号生成规则
- 订单状态流转
- 师傅接单逻辑
- 订单评价逻辑

---

## 📝 接口设计建议

### 订单状态流转

```
待接单 → 已接单 → 进行中 → 已完成
  ↓
已取消
```

### 订单号生成规则

建议格式：`SO + 日期 + 序号`
例如：`SO20250124001`

### 服务类型枚举

- `安装` - 电器安装
- `维修` - 电器维修
- `清洗` - 电器清洗
- `保养` - 电器保养
- `其他` - 其他服务

---

## ⚠️ 注意事项

1. **当前状态：** 所有接口仅返回提示信息，不执行任何业务逻辑
2. **API文档：** 接口已在Swagger文档中显示，标注为"预留接口"
3. **扩展性：** 接口设计考虑了未来可能的需求，便于扩展
4. **数据库：** 建议在需要时再创建相关表，避免过早设计

---

## 📚 参考

- 接口定义：`ServiceOrderController.java`
- DTO定义：`ServiceOrderDTO.java`、`ServiceOrderVO.java`
- Service接口：`ServiceOrderService.java`

---

**预留接口已创建，方便未来二次开发扩展！** 🎉
