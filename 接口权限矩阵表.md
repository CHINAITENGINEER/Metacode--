# 接口权限矩阵表

## 📊 完整权限矩阵

| 模块 | 接口路径 | 方法 | 接口说明 | 管理员 | 店员 | 权限注解 |
|------|---------|------|---------|--------|------|---------|
| **认证** | `/auth/login` | POST | 管理员登录 | ✅ | ❌ | 无（公开） |
| | `/auth/staff-login` | POST | 店员登录 | ❌ | ✅ | 无（公开） |
| **数据大屏** | `/dashboard/stats` | GET | 核心指标统计 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/dashboard/trends` | GET | 趋势数据 | ✅ | ❌ | `@RequireRole("admin")` |
| **会员管理** | `/members/list` | GET | 会员列表 | ✅ | ✅ | `@RequireRole({"admin", "staff"})` |
| | `/members/{id}` | GET | 会员详情 | ✅ | ✅ | `@RequireRole({"admin", "staff"})` |
| | `/members` | POST | 创建会员 | ✅ | ✅ | `@RequireRole({"admin", "staff"})` |
| | `/members/{id}/points/adjust` | POST | 调整积分 | ✅ | ✅ | `@RequireRole({"admin", "staff"})` |
| **积分记录** | `/points-records/list` | GET | 积分记录列表 | ✅ | ✅* | `@RequireRole({"admin", "staff"})` |
| **店员管理** | `/staffs/list` | GET | 店员列表 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/staffs/{id}` | GET | 店员详情 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/staffs` | POST | 创建店员 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/staffs/{id}/reset-password` | POST | 重置密码 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/staffs/{id}/toggle-status` | POST | 启用/禁用 | ✅ | ❌ | `@RequireRole("admin")` |
| **导出功能** | `/export/points-records` | GET | 导出积分记录 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/export/consumption-records` | GET | 导出消费记录 | ✅ | ❌ | `@RequireRole("admin")` |
| | `/export/financial-report` | GET | 导出财务报表 | ✅ | ❌ | `@RequireRole("admin")` |

**说明**：
- ✅ 可以访问
- ❌ 不能访问（会返回权限错误）
- ✅* 可以访问，但数据权限不同（店员只能查看自己操作的记录）

---

## 🔐 权限统计

### 管理员权限
- **总接口数**：17个
- **可访问接口**：17个（100%）
- **权限范围**：所有功能

### 店员权限
- **总接口数**：17个
- **可访问接口**：7个（41%）
- **权限范围**：
  - ✅ 会员管理（4个接口）
  - ✅ 积分记录查询（1个接口，数据权限受限）
  - ✅ 登录接口（1个接口）
  - ✅ 测试接口（1个接口）

---

## 📋 按角色分类的接口列表

### 管理员专用接口（10个）

1. `GET /dashboard/stats` - 数据大屏统计
2. `GET /dashboard/trends` - 趋势数据
3. `GET /staffs/list` - 店员列表
4. `GET /staffs/{id}` - 店员详情
5. `POST /staffs` - 创建店员
6. `POST /staffs/{id}/reset-password` - 重置密码
7. `POST /staffs/{id}/toggle-status` - 启用/禁用
8. `GET /export/points-records` - 导出积分记录
9. `GET /export/consumption-records` - 导出消费记录
10. `GET /export/financial-report` - 导出财务报表

### 管理员和店员共享接口（5个）

1. `GET /members/list` - 会员列表
2. `GET /members/{id}` - 会员详情
3. `POST /members` - 创建会员
4. `POST /members/{id}/points/adjust` - 调整积分
5. `GET /points-records/list` - 积分记录列表

**注意**：积分记录列表虽然管理员和店员都可以访问，但：
- **管理员**：可以查看所有会员的积分记录
- **店员**：只能查看自己操作的积分记录

### 公开接口（2个）

1. `POST /auth/login` - 管理员登录
2. `POST /auth/staff-login` - 店员登录

---

## 🎯 权限验证示例

### 场景1：管理员访问数据大屏
```
请求：GET /dashboard/stats
Token: Bearer <admin_token>
角色：admin
结果：✅ 成功（角色匹配）
```

### 场景2：店员访问数据大屏
```
请求：GET /dashboard/stats
Token: Bearer <staff_token>
角色：staff
结果：❌ 失败（返回：无权限访问此接口，需要角色：[admin]）
```

### 场景3：店员访问会员列表
```
请求：GET /members/list
Token: Bearer <staff_token>
角色：staff
结果：✅ 成功（角色匹配）
```

### 场景4：店员查看积分记录
```
请求：GET /points-records/list
Token: Bearer <staff_token>
角色：staff
结果：✅ 成功（但只能看到自己操作的记录）
```

---

## 🔧 权限控制实现位置

### 代码位置

1. **权限注解**：`backend/huakang-service/src/main/java/com/huakang/service/annotation/RequireRole.java`
2. **拦截器**：`backend/huakang-service/src/main/java/com/huakang/service/interceptor/RoleCheckInterceptor.java`
3. **配置**：`backend/huakang-admin/src/main/java/com/huakang/admin/config/WebMvcConfig.java`
4. **Controller注解**：各个Controller类上

### 权限检查流程

```
HTTP请求
  ↓
Spring Security（基础认证）
  ↓
RoleCheckInterceptor（角色检查）
  ↓
检查 @RequireRole 注解
  ↓
从JWT Token获取角色
  ↓
验证角色是否匹配
  ↓
通过：继续执行Controller
失败：抛出 BusinessException
```

---

## 📝 注意事项

1. **Token必须有效**：JWT Token必须包含 `role` 字段
2. **角色值必须匹配**：Token中的角色值必须是 `admin` 或 `staff`（小写）
3. **数据权限**：接口权限控制访问，数据权限在Service层实现
4. **错误提示**：权限不足时会返回业务异常，错误码1000

---

## 🧪 测试建议

### 测试管理员权限
1. 使用管理员账号登录
2. 测试所有接口，应该都能成功

### 测试店员权限
1. 使用店员账号登录
2. 测试会员管理接口，应该成功
3. 测试数据大屏接口，应该失败
4. 测试店员管理接口，应该失败
5. 测试导出功能接口，应该失败

### 测试权限错误提示
1. 使用店员账号访问管理员接口
2. 应该返回：`{"code": 1000, "message": "无权限访问此接口，需要角色：[admin]", "data": null}`
