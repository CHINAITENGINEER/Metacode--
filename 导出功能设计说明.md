# 导出功能设计说明

## 📋 功能概述

为财务统计提供导出功能，支持导出：
1. **积分记录** - 所有积分变动记录
2. **真钱消费记录** - 消费赠送积分对应的真钱消费记录
3. **财务统计报表** - 汇总统计报表

---

## 🎯 业务逻辑

### 1. 积分记录导出

**数据来源：** `points_records` 表

**导出字段：**
- 记录ID
- 会员昵称
- 会员手机号
- 变动类型（消费赠送/积分兑换/后台调整等）
- 变动分值（正数=增加，负数=扣除）
- 变动前余额
- 变动后余额
- 操作人类型（admin/staff/system）
- 操作人姓名
- 业务单号
- 备注
- 操作时间

**筛选条件：**
- 时间范围（必填）
- 会员ID（可选）

---

### 2. 真钱消费记录导出

**数据来源：** `points_records` 表中 `change_type = '消费赠送'` 的记录

**业务逻辑：**
- 真钱消费记录 = 消费赠送积分对应的消费
- 消费金额 = 赠送积分 × 积分比例（从系统配置读取）
- 默认比例：1元 = 1积分（可在系统配置中调整）

**导出字段：**
- 记录ID
- 会员昵称
- 会员手机号
- 消费金额（元）
- 赠送积分
- 业务单号
- 操作人
- 备注
- 消费时间

**筛选条件：**
- 时间范围（必填）
- 会员ID（可选）

---

### 3. 财务统计报表导出

**数据来源：** `points_records` 表统计汇总

**统计项：**
1. **真钱消费总额**
   - 金额：所有"消费赠送"记录的消费金额总和
   - 积分：所有"消费赠送"记录的积分总和
   - 笔数：消费赠送记录数量

2. **累计发放积分**
   - 积分：所有增加积分的记录总和
   - 笔数：增加积分记录数量

3. **累计消耗积分**
   - 积分：所有扣除积分的记录总和
   - 笔数：扣除积分记录数量

4. **积分兑换总额**
   - 积分：所有"积分兑换"记录的积分总和
   - 笔数：积分兑换记录数量
   - 金额：兑换商品对应的价值（如果关联了商品）

**导出格式：**
- 统计项
- 金额（元）
- 积分
- 笔数
- 备注

---

## 📊 技术实现

### 1. 依赖

使用 **EasyExcel** 进行Excel导出：

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>easyexcel</artifactId>
    <version>3.3.2</version>
</dependency>
```

### 2. 接口设计

**导出积分记录：**
```
GET /api/admin/export/points-records
参数：
  - startTime: 开始时间（必填）
  - endTime: 结束时间（必填）
  - memberId: 会员ID（可选）
```

**导出真钱消费记录：**
```
GET /api/admin/export/consumption-records
参数：
  - startTime: 开始时间（必填）
  - endTime: 结束时间（必填）
  - memberId: 会员ID（可选）
```

**导出财务统计报表：**
```
GET /api/admin/export/financial-report
参数：
  - startTime: 开始时间（必填）
  - endTime: 结束时间（必填）
```

### 3. 实现要点

#### 3.1 积分比例配置

真钱消费金额 = 赠送积分 × 积分比例

积分比例从系统配置表读取：
```sql
SELECT config_value FROM system_configs 
WHERE config_key = 'points_consumption_ratio';
-- 默认值：0.01（1元=0.01积分，即100元=1积分）
```

**注意：** 当前代码中默认使用 1:1 比例，需要从配置读取。

#### 3.2 文件名格式

- 积分记录：`积分记录_20250101_20250131.xlsx`
- 真钱消费记录：`真钱消费记录_20250101_20250131.xlsx`
- 财务统计报表：`财务统计报表_20250101_20250131.xlsx`

#### 3.3 性能优化

- 大数据量时考虑分页查询
- 使用流式导出（EasyExcel支持）
- 添加导出任务队列（可选，异步导出）

---

## 🔧 代码结构

### Service层

```
huakang-service/
├── service/
│   └── ExportService.java          # 导出服务接口
├── impl/
│   └── ExportServiceImpl.java     # 导出服务实现
└── dto/export/
    ├── PointsRecordExportDTO.java   # 积分记录导出DTO
    ├── ConsumptionRecordExportDTO.java  # 消费记录导出DTO
    └── FinancialReportDTO.java      # 财务报表DTO
```

### Controller层

```
huakang-admin/
└── controller/
    └── ExportController.java        # 导出Controller
```

---

## ⚠️ 注意事项

### 1. 积分比例配置

**当前实现：** 默认使用 1:1 比例（1元=1积分）

**需要优化：** 从系统配置表读取 `points_consumption_ratio` 配置

**修改位置：** `ExportServiceImpl.java` 中的 `consumptionRatio` 变量

### 2. 积分兑换商品价值

**当前实现：** 积分兑换金额为0（需要关联商品表查询）

**需要优化：** 如果积分记录关联了商品（`product_id`），从商品表查询价格

### 3. 大数据量处理

**建议：**
- 数据量 > 1万条时，使用流式导出
- 数据量 > 10万条时，考虑异步导出（生成任务，完成后下载）

### 4. 权限控制

**建议：** 导出功能仅管理员可用，店员无权限

---

## 📝 使用示例

### 1. 导出积分记录

```bash
# 导出2025年1月的所有积分记录
GET /api/admin/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59

# 导出指定会员的积分记录
GET /api/admin/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59&memberId=123
```

### 2. 导出真钱消费记录

```bash
# 导出2025年1月的真钱消费记录
GET /api/admin/export/consumption-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59
```

### 3. 导出财务统计报表

```bash
# 导出2025年1月的财务统计报表
GET /api/admin/export/financial-report?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59
```

---

## 🔄 后续优化建议

### 1. 异步导出（大数据量）

```java
// 创建导出任务
POST /api/admin/export/tasks
{
  "exportType": "points_records",
  "startTime": "2025-01-01 00:00:00",
  "endTime": "2025-01-31 23:59:59"
}

// 查询导出任务状态
GET /api/admin/export/tasks/{taskId}

// 下载导出文件
GET /api/admin/export/tasks/{taskId}/download
```

### 2. 导出模板自定义

- 支持自定义导出字段
- 支持自定义Excel模板

### 3. 导出历史记录

- 记录导出历史
- 支持重复下载

---

## ✅ 已完成

- ✅ ExportService接口定义
- ✅ ExportServiceImpl实现
- ✅ ExportController接口
- ✅ 导出DTO定义
- ✅ EasyExcel依赖配置

## ⚠️ 待优化

- [ ] 从系统配置读取积分比例
- [ ] 积分兑换商品价值计算（关联商品表）
- [ ] 权限控制（仅管理员）
- [ ] 大数据量流式导出
- [ ] 文件名中文编码处理

---

**导出功能已实现，可以开始测试！** 🎉
