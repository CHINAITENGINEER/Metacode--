# 权限控制快速参考

## 🎯 一句话总结

- **管理员（admin）**：可以访问所有17个接口
- **店员（staff）**：只能访问7个接口（会员管理4个 + 积分记录1个 + 登录1个 + 测试1个）

---

## 📊 权限速查表

### ✅ 管理员可以访问（17个接口）

| 模块 | 接口数 | 说明 |
|------|--------|------|
| 认证 | 1 | 管理员登录 |
| 数据大屏 | 2 | 统计和趋势 |
| 会员管理 | 4 | 列表、详情、创建、积分操作 |
| 积分记录 | 1 | 查看所有记录 |
| 店员管理 | 5 | 列表、详情、创建、重置密码、启用/禁用 |
| 导出功能 | 3 | 积分记录、消费记录、财务报表 |
| 其他 | 1 | 测试接口 |

### ✅ 店员可以访问（7个接口）

| 模块 | 接口数 | 说明 |
|------|--------|------|
| 认证 | 1 | 店员登录 |
| 会员管理 | 4 | 列表、详情、创建、积分操作 |
| 积分记录 | 1 | 只能查看自己操作的记录 |
| 其他 | 1 | 测试接口 |

### ❌ 店员不能访问（10个接口）

1. ❌ `GET /dashboard/stats` - 数据大屏统计
2. ❌ `GET /dashboard/trends` - 趋势数据
3. ❌ `GET /staffs/list` - 店员列表
4. ❌ `GET /staffs/{id}` - 店员详情
5. ❌ `POST /staffs` - 创建店员
6. ❌ `POST /staffs/{id}/reset-password` - 重置密码
7. ❌ `POST /staffs/{id}/toggle-status` - 启用/禁用
8. ❌ `GET /export/points-records` - 导出积分记录
9. ❌ `GET /export/consumption-records` - 导出消费记录
10. ❌ `GET /export/financial-report` - 导出财务报表

---

## 🔍 权限检查机制

### 实现方式
- 使用 `@RequireRole` 注解标记Controller
- 通过 `RoleCheckInterceptor` 拦截器自动检查
- 从JWT Token中获取用户角色进行验证

### 权限不足时的响应
```json
{
  "code": 1000,
  "message": "无权限访问此接口，需要角色：[admin]",
  "data": null
}
```

---

## 📝 代码示例

### Controller权限配置

```java
// 仅管理员
@RequireRole("admin")
public class DashboardController { ... }

// 管理员和店员都可以
@RequireRole({"admin", "staff"})
public class MemberController { ... }
```

---

## 🧪 测试权限

### 使用管理员Token
- ✅ 所有接口都能访问

### 使用店员Token
- ✅ 会员管理接口：可以访问
- ✅ 积分记录接口：可以访问（但只能看自己的）
- ❌ 数据大屏接口：权限不足
- ❌ 店员管理接口：权限不足
- ❌ 导出功能接口：权限不足
