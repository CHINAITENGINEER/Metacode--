# 导出功能使用说明

## 📋 功能概述

为财务统计提供Excel导出功能，支持导出：
1. **积分记录** - 所有积分变动记录
2. **真钱消费记录** - 消费赠送积分对应的真钱消费记录
3. **财务统计报表** - 汇总统计报表

---

## 🎯 业务逻辑说明

### 1. 积分记录导出

**数据来源：** `points_records` 表的所有记录

**导出内容：**
- 记录ID、会员信息（昵称、手机号）
- 变动类型、变动分值、余额快照
- 操作人信息、业务单号、备注、时间

**使用场景：**
- 财务对账
- 积分流水审计
- 会员积分查询

---

### 2. 真钱消费记录导出

**数据来源：** `points_records` 表中 `change_type = '消费赠送'` 的记录

**业务逻辑：**
- 真钱消费记录 = 消费赠送积分对应的消费
- **消费金额计算公式：** 消费金额 = 赠送积分 ÷ 积分比例
- **积分比例：** 从系统配置表读取 `points_consumption_ratio`
  - 默认值：0.01（即 1元 = 0.01积分，100元 = 1积分）
  - 可在系统配置中修改

**导出内容：**
- 会员信息、消费金额（元）、赠送积分
- 业务单号、操作人、备注、消费时间

**使用场景：**
- 财务统计真钱收入
- 消费记录对账
- 销售数据分析

---

### 3. 财务统计报表导出

**数据来源：** `points_records` 表统计汇总

**统计项：**
1. **真钱消费总额**
   - 金额：所有"消费赠送"记录的消费金额总和
   - 积分：所有"消费赠送"记录的积分总和
   - 笔数：消费赠送记录数量

2. **累计发放积分**
   - 积分：所有增加积分的记录总和
   - 笔数：增加积分记录数量

3. **累计消耗积分**
   - 积分：所有扣除积分的记录总和
   - 笔数：扣除积分记录数量

4. **积分兑换总额**
   - 积分：所有"积分兑换"记录的积分总和
   - 笔数：积分兑换记录数量

**使用场景：**
- 月度/年度财务报表
- 财务汇总统计
- 经营数据分析

---

## 📡 API接口

### 1. 导出积分记录

**接口：** `GET /api/admin/export/points-records`

**参数：**
- `startTime` (必填) - 开始时间，格式：`yyyy-MM-dd HH:mm:ss`
- `endTime` (必填) - 结束时间，格式：`yyyy-MM-dd HH:mm:ss`
- `memberId` (可选) - 会员ID，不传则导出所有会员

**示例：**
```bash
# 导出2025年1月的所有积分记录
GET /api/admin/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59

# 导出指定会员的积分记录
GET /api/admin/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59&memberId=123
```

**响应：** Excel文件（.xlsx）

**文件名：** `积分记录_20250101_20250131.xlsx`

---

### 2. 导出真钱消费记录

**接口：** `GET /api/admin/export/consumption-records`

**参数：**
- `startTime` (必填) - 开始时间
- `endTime` (必填) - 结束时间
- `memberId` (可选) - 会员ID

**示例：**
```bash
# 导出2025年1月的真钱消费记录
GET /api/admin/export/consumption-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59
```

**响应：** Excel文件（.xlsx）

**文件名：** `真钱消费记录_20250101_20250131.xlsx`

---

### 3. 导出财务统计报表

**接口：** `GET /api/admin/export/financial-report`

**参数：**
- `startTime` (必填) - 开始时间
- `endTime` (必填) - 结束时间

**示例：**
```bash
# 导出2025年1月的财务统计报表
GET /api/admin/export/financial-report?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59
```

**响应：** Excel文件（.xlsx）

**文件名：** `财务统计报表_20250101_20250131.xlsx`

---

## 📊 Excel格式说明

### 积分记录Excel格式

| 记录ID | 会员昵称 | 会员手机号 | 变动类型 | 变动分值 | 变动前余额 | 变动后余额 | 操作人类型 | 操作人姓名 | 业务单号 | 备注 | 操作时间 |
|--------|---------|-----------|---------|---------|-----------|-----------|-----------|-----------|---------|------|---------|
| 1 | 王女士 | 138****1234 | 消费赠送 | +1000 | 0 | 1000 | staff | 店员A | ORDER001 | 消费赠送 | 2025-01-15 10:30:00 |

---

### 真钱消费记录Excel格式

| 记录ID | 会员昵称 | 会员手机号 | 消费金额（元） | 赠送积分 | 业务单号 | 操作人 | 备注 | 消费时间 |
|--------|---------|-----------|--------------|---------|---------|--------|------|---------|
| 1 | 王女士 | 138****1234 | 100000.00 | 1000 | ORDER001 | 店员A | 消费赠送 | 2025-01-15 10:30:00 |

**说明：** 消费金额 = 赠送积分 ÷ 积分比例（默认：1000 ÷ 0.01 = 100000元）

---

### 财务统计报表Excel格式

| 统计项 | 金额（元） | 积分 | 笔数 | 备注 |
|--------|----------|------|------|------|
| 真钱消费总额 | 1000000.00 | 10000 | 50 | 消费赠送积分对应的真钱消费 |
| 累计发放积分 | 0.00 | 15000 | 60 | 所有增加积分的记录 |
| 累计消耗积分 | 0.00 | 5000 | 20 | 所有扣除积分的记录 |
| 积分兑换总额 | 0.00 | 3000 | 10 | 积分兑换商品对应的价值 |

---

## ⚙️ 配置说明

### 积分比例配置

**配置键：** `points_consumption_ratio`

**配置值：** 积分比例（例如：0.01 表示 1元 = 0.01积分）

**默认值：** 0.01（即 100元 = 1积分）

**修改方式：**
1. 在系统配置表中修改
2. 或在后台管理系统设置中修改

**计算公式：**
- 消费金额 = 赠送积分 ÷ 积分比例
- 例如：赠送1000积分，比例0.01，则消费金额 = 1000 ÷ 0.01 = 100,000元

---

## 🔧 技术实现

### 依赖

使用 **EasyExcel 3.3.2** 进行Excel导出：

```xml
<dependency>
    <groupId>com.alibaba</groupId>
    <artifactId>easyexcel</artifactId>
    <version>3.3.2</version>
</dependency>
```

### 代码结构

```
backend/
├── huakang-service/
│   ├── service/
│   │   └── ExportService.java              # 导出服务接口
│   ├── impl/
│   │   └── ExportServiceImpl.java         # 导出服务实现
│   └── dto/export/
│       ├── PointsRecordExportDTO.java      # 积分记录导出DTO
│       ├── ConsumptionRecordExportDTO.java # 消费记录导出DTO
│       └── FinancialReportDTO.java         # 财务报表DTO
└── huakang-admin/
    └── controller/
        └── ExportController.java           # 导出Controller
```

---

## ⚠️ 注意事项

### 1. 权限控制

**建议：** 导出功能仅管理员可用，店员无权限

**实现方式：** 在Controller上添加权限注解
```java
@PreAuthorize("hasRole('ADMIN')")
```

### 2. 大数据量处理

**当前实现：** 一次性查询所有数据并导出

**优化建议：**
- 数据量 > 1万条：使用流式导出
- 数据量 > 10万条：考虑异步导出（生成任务，完成后下载）

### 3. 文件名编码

**已处理：** 文件名使用URL编码，支持中文文件名

### 4. 积分比例

**当前实现：** 从系统配置读取，默认0.01

**注意：** 如果配置不存在或读取失败，使用默认值0.01

---

## 📝 使用示例

### 前端调用示例（JavaScript）

```javascript
// 导出积分记录
function exportPointsRecords(startTime, endTime, memberId) {
    const params = new URLSearchParams({
        startTime: startTime,
        endTime: endTime
    });
    if (memberId) {
        params.append('memberId', memberId);
    }
    
    window.open(`/api/admin/export/points-records?${params.toString()}`);
}

// 导出真钱消费记录
function exportConsumptionRecords(startTime, endTime) {
    const params = new URLSearchParams({
        startTime: startTime,
        endTime: endTime
    });
    window.open(`/api/admin/export/consumption-records?${params.toString()}`);
}

// 导出财务统计报表
function exportFinancialReport(startTime, endTime) {
    const params = new URLSearchParams({
        startTime: startTime,
        endTime: endTime
    });
    window.open(`/api/admin/export/financial-report?${params.toString()}`);
}
```

### Postman测试

1. **导出积分记录**
   - Method: GET
   - URL: `http://localhost:8080/api/admin/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59`
   - 点击"Send and Download"

2. **导出真钱消费记录**
   - Method: GET
   - URL: `http://localhost:8080/api/admin/export/consumption-records?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59`

3. **导出财务统计报表**
   - Method: GET
   - URL: `http://localhost:8080/api/admin/export/financial-report?startTime=2025-01-01 00:00:00&endTime=2025-01-31 23:59:59`

---

## 🔄 后续优化建议

### 1. 异步导出（大数据量）

```java
// 创建导出任务
POST /api/admin/export/tasks
{
  "exportType": "points_records",
  "startTime": "2025-01-01 00:00:00",
  "endTime": "2025-01-31 23:59:59"
}

// 查询任务状态
GET /api/admin/export/tasks/{taskId}

// 下载文件
GET /api/admin/export/tasks/{taskId}/download
```

### 2. 导出模板自定义

- 支持自定义导出字段
- 支持自定义Excel模板

### 3. 导出历史记录

- 记录导出历史
- 支持重复下载

### 4. 多Sheet导出

- 积分记录和消费记录合并到一个Excel的多个Sheet

---

## ✅ 已完成功能

- ✅ 积分记录导出
- ✅ 真钱消费记录导出
- ✅ 财务统计报表导出
- ✅ 从系统配置读取积分比例
- ✅ 文件名中文编码处理
- ✅ EasyExcel集成

---

## 🎯 使用场景

### 财务月度对账

1. 导出当月积分记录 → 核对积分流水
2. 导出当月真钱消费记录 → 核对销售收入
3. 导出财务统计报表 → 生成月度报表

### 年度财务统计

1. 导出全年积分记录 → 年度积分统计
2. 导出全年真钱消费记录 → 年度收入统计
3. 导出财务统计报表 → 年度财务报表

---

**导出功能已实现，可以开始测试使用！** 🎉
