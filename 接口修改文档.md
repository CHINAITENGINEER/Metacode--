# 接口修改文档

**更新日期：** 2026-02-12
**版本：** v1.2.0

---

## 一、新增接口

### 1. 查询下架商品列表

**接口地址：** `GET /products/offline`

**权限要求：** 仅管理员

**使用场景：** 查询已下架的商品列表（`is_deleted=1`）

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | Integer | 否 | 页码，默认1 | 1 |
| size | Integer | 否 | 每页大小，默认10 | 10 |
| type | Integer | 否 | 商品类型：1=全量商品，2=积分商品 | 1 |
| category | String | 否 | 商品分类 | 家用电器 |
| name | String | 否 | 商品名称（模糊查询） | 冰箱 |

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 15,
    "pages": 2,
    "current": 1,
    "size": 10,
    "records": [
      {
        "id": 1,
        "name": "海尔冰箱",
        "image": "https://xxx.com/image.jpg",
        "category": "家用电器",
        "type": 1,
        "price": 2999.00,
        "status": 1,
        "sortOrder": 100,
        "createdAt": "2025-01-01T10:00:00",
        "updatedAt": "2025-01-15T14:30:00"
      }
    ]
  }
}
```

---

### 2. 上架商品（恢复商品）

**接口地址：** `PUT /products/{id}/online`

**权限要求：** 仅管理员

**使用场景：** 将下架的商品重新上架（恢复is_deleted=0）

**路径参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | 是 | 商品ID |

**响应示例：**
```json
{
  "code": 200,
  "message": "上架成功",
  "data": null
}
```

**前端建议：**
- 在下架商品列表中，为每个商品提供"上架"按钮
- 点击后调用此接口，成功后刷新列表

---

### 3. 批量上传图片和视频

**接口地址：** `POST /products/upload-batch`

**权限要求：** 仅管理员

**使用场景：** 商品上传时，支持一次性批量上传多张图片和视频，支持拖拽上传

**请求参数：**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| files | MultipartFile[] | 是 | 文件列表（可包含图片和视频） |

**支持格式：**
- **图片：** JPG、PNG、GIF、WebP
- **视频：** MP4、MOV、AVI、WMV、MPEG

**文件大小限制：**
- 图片：单个最大10MB
- 视频：单个最大100MB

**响应示例：**
```json
{
  "code": 200,
  "message": "批量上传成功",
  "data": [
    "https://oss.example.com/products/20260211/abc123.jpg",
    "https://oss.example.com/products/20260211/def456.jpg",
    "https://oss.example.com/products/20260211/video123.mp4",
    "https://oss.example.com/products/20260211/ghi789.jpg"
  ]
}
```

**前端实现建议：**

1. **拖拽上传实现**
   - 使用 `react-dropzone` 或原生 HTML5 拖拽API
   - 支持多文件选择和拖拽（可同时选择图片和视频）
   - 显示上传进度和预览

2. **文件顺序控制**
   - 上传后的URL列表按上传顺序返回
   - 前端可通过拖拽调整图片/视频顺序
   - 调整后的URL列表保存到 `detailImages` 字段

3. **保存商品时**
   - 将文件URL数组按顺序传给 `detailImages` 字段
   - 创建商品：`POST /products`，body中包含 `detailImages: ["url1", "url2", "videoUrl", "url3"]`
   - 更新商品：`PUT /products/{id}`，body中包含 `detailImages: ["url1", "url2", "videoUrl", "url3"]`

4. **文件类型识别**
   - 前端根据URL后缀判断是图片还是视频
   - 图片显示为图片，视频显示为视频播放器

**示例代码片段：**
```javascript
// 批量上传图片和视频
const uploadMediaFiles = async (files) => {
  const formData = new FormData();
  files.forEach(file => {
    formData.append('files', file);
  });
  
  const response = await fetch('/products/upload-batch', {
    method: 'POST',
    body: formData
  });
  
  const result = await response.json();
  return result.data; // 返回URL数组（按上传顺序）
};

// 拖拽调整顺序后保存
const saveProduct = async (productData, mediaUrls) => {
  await fetch('/products', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      ...productData,
      detailImages: mediaUrls // 按拖拽后的顺序，可包含图片和视频URL
    })
  });
};

// 判断是图片还是视频
const isVideo = (url) => {
  return /\.(mp4|mov|avi|wmv|mpeg)$/i.test(url);
};

// 渲染媒体文件
const renderMedia = (url) => {
  if (isVideo(url)) {
    return <video src={url} controls />;
  } else {
    return <img src={url} alt="商品图片" />;
  }
};
```

---

## 二、修改接口

### 1. 调整积分接口（新增商品关联参数和返回商品名称）

**接口地址：** `POST /members/{id}/points/adjust`

**权限要求：** 管理员、店员

**变更说明：** 
1. 新增 `productId` 可选参数，用于关联积分变动对应的商品
2. 返回参数新增 `productName` 字段，如果关联了商品会返回商品名称

**请求参数（新增）：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| productId | Long | 否 | 关联商品ID（如果本次积分变动与商品相关） | 1 |

**完整请求参数：**

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| type | String | 是 | 变动类型：add=增加，subtract=扣除，set=设置 | add |
| points | Integer | 是 | 积分值 | 100 |
| remark | String | 是 | 备注/原因 | 购买商品赠送 |
| productId | Long | 否 | 关联商品ID（新增） | 1 |

**请求示例：**
```json
{
  "type": "add",
  "points": 100,
  "remark": "购买商品赠送",
  "productId": 1
}
```

**响应参数（新增）：**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| productName | String | 关联商品名称（如果请求中传入了productId，会返回商品名称；否则为null） | 海尔冰箱 |

**响应示例：**
```json
{
  "code": 200,
  "message": "积分调整成功",
  "data": {
    "id": 1,
    "openid": "oxxxxxxxxxxxxxxxxxxxxxx",
    "nickname": "张三",
    "avatar": "https://example.com/avatar.jpg",
    "phone": "13800138000",
    "totalPoints": 1100,
    "createdAt": "2025-01-01T10:00:00",
    "updatedAt": "2025-01-15T14:30:00",
    "productName": "海尔冰箱"
  }
}
```

**说明：**
- `productId` 为可选参数，如果本次积分变动与商品相关（如购买商品赠送积分、积分兑换商品等），可以传入商品ID
- 传入 `productId` 后，积分记录中会保存该商品ID，便于后续查询和统计
- 如果不传入 `productId`，积分记录中的 `productId` 字段为 null
- **返回参数新增 `productName` 字段**：如果请求中传入了 `productId`，返回的会员信息中会包含关联的商品名称；如果未传入 `productId`，`productName` 为 null

---

### 2. 下架商品接口

**接口地址：** `DELETE /products/{id}`

**变更说明：** 下架商品使用逻辑删除（`is_deleted=1`）

**说明：**
- 下架商品：设置 `is_deleted=1`，商品仍然存在，只是不可见
- 上架商品：调用 `PUT /products/{id}/online` 接口，恢复 `is_deleted=0`

---

### 4. 积分记录列表（新增字段）

**接口地址：** `GET /points-records/list`

**变更说明：** 返回数据新增 `productId` 和 `productName` 字段

**新增响应字段：**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| productId | Long | 关联商品ID（可能为null） | 1 |
| productName | String | 关联商品名称（可能为null） | 海尔冰箱 |

**完整响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "records": [
      {
        "id": 1,
        "memberId": 10,
        "memberNickname": "张三",
        "memberPhone": "13800138000",
        "changeType": "积分兑换",
        "points": -500,
        "balanceBefore": 1000,
        "balanceAfter": 500,
        "operatorType": "system",
        "operatorName": "系统",
        "createdAt": "2025-01-01T10:00:00",
        "remark": "兑换商品",
        "productId": 1,
        "productName": "海尔冰箱"
      }
    ]
  }
}
```

**前端建议：**
- 在会员积分记录列表中新增"关联电器"列
- 如果 `productName` 为 null，显示为 "-" 或空
- 旧数据中 `productId` 可能为 null，需做空值处理

---

### 5. 导出积分记录（新增列）

**接口地址：** `GET /export/points-records`

**变更说明：** 导出的Excel文件新增"关联商品"列

**Excel列顺序：**

| 列序号 | 列名 | 说明 |
|--------|------|------|
| 0 | 记录ID | - |
| 1 | 会员昵称 | - |
| 2 | 会员手机号 | - |
| 3 | 变动类型 | - |
| 4 | 变动分值 | - |
| 5 | 变动前余额 | - |
| 6 | 变动后余额 | - |
| 7 | 操作人类型 | - |
| 8 | 操作人姓名 | - |
| 9 | 业务单号 | - |
| 10 | 备注 | - |
| 11 | **关联商品** | **新增列** |
| 12 | 操作时间 | - |

**前端建议：**
- 导出前可增加文件命名弹窗，让用户自定义文件名
- 后端默认文件名格式：`积分记录_开始日期_结束日期.xlsx`
- 可使用浏览器的"另存为"功能让用户修改文件名

---

## 三、注意事项

1. **批量上传：** 支持同时上传图片和视频，URL按上传顺序返回，前端可通过拖拽调整顺序后保存到 `detailImages`
2. **文件类型：** 后端自动识别文件类型（图片/视频），无需前端区分
3. **文件大小：** 图片最大10MB，视频最大100MB
4. **支持格式：** 图片支持JPG/PNG/GIF/WebP，视频支持MP4/MOV/AVI/WMV/MPEG
5. **混合存储：** `detailImages` 数组可同时包含图片和视频URL，前端根据URL后缀判断类型并渲染
6. **商品下架和上架：**
   - 下架：调用 `DELETE /products/{id}`，同时设置 `is_deleted=1`（逻辑删除）和 `status=0`（下架）
   - 上架：调用 `PUT /products/{id}/online`，同时恢复 `is_deleted=0` 和 `status=1`（上架）
   - 下架商品列表：调用 `GET /products/offline`，查询 `is_deleted=1` 的商品
   - 小程序查询：使用 `status=1` 查询上架商品，`is_deleted=0` 过滤已删除商品
7. **调整积分商品关联：**
   - 调整积分接口新增 `productId` 可选参数
   - 如果积分变动与商品相关（如购买商品赠送积分、积分兑换商品），建议传入 `productId`
   - 传入 `productId` 后，积分记录中会保存商品关联信息，便于后续查询和统计
7. **兼容性：** 旧数据中 `product_id` 可能为 null，前端需做空值处理
8. **权限控制：** 所有商品管理接口仅管理员可访问
9. **拖拽排序：** 图片/视频的顺序由前端控制，通过 `detailImages` 数组顺序体现

---

## 四、数据库变更

**本次更新：** 无需修改数据库

**说明：**
- `points_records` 表已有 `product_id` 字段，可直接使用
- `products` 表的 `detail_images` 字段为JSON类型，可同时存储图片和视频URL

---

**文档版本：** v1.1  
**最后更新：** 2026-02-12
