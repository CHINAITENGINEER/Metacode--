# 华康电器连锁积分小程序 - 数据库设计文档

## 1. 数据库概述

- **数据库名称**: hk_electronics
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_unicode_ci
- **用途**: 华康电器连锁积分小程序后端服务数据库

## 2. 数据表结构

### 2.1 管理员表 (admins)

存储系统管理员信息。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| username | VARCHAR(50) | NOT NULL | - | 登录账号 |
| password_hash | VARCHAR(255) | NOT NULL | - | 密码哈希值（BCrypt） |
| name | VARCHAR(100) | NOT NULL | - | 管理员姓名 |
| email | VARCHAR(100) | NULL | - | 邮箱 |
| phone | VARCHAR(20) | NULL | - | 手机号 |
| role | VARCHAR(20) | NOT NULL | 'admin' | 角色（预留） |
| last_login_at | DATETIME | NULL | - | 最后登录时间 |
| last_login_ip | VARCHAR(50) | NULL | - | 最后登录IP |
| login_fail_count | TINYINT UNSIGNED | NOT NULL | 0 | 连续登录失败次数 |
| locked_until | DATETIME | NULL | - | 账号锁定到期时间 |
| is_deleted | TINYINT UNSIGNED | NOT NULL | 0 | 是否删除：0=否，1=是 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `uk_username` (`username`)
- KEY: `idx_is_deleted` (`is_deleted`)
- KEY: `idx_created_at` (`created_at`)

### 2.2 店员表 (staffs)

存储店员信息。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| username | VARCHAR(50) | NOT NULL | - | 登录账号 |
| password_hash | VARCHAR(255) | NOT NULL | - | 密码哈希值（BCrypt） |
| name | VARCHAR(100) | NOT NULL | - | 店员姓名 |
| email | VARCHAR(100) | NULL | - | 邮箱 |
| phone | VARCHAR(20) | NULL | - | 手机号 |
| department | VARCHAR(50) | NULL | - | 部门（预留） |
| status | TINYINT UNSIGNED | NOT NULL | 1 | 状态：1=启用，0=禁用 |
| last_login_at | DATETIME | NULL | - | 最后登录时间 |
| last_login_ip | VARCHAR(50) | NULL | - | 最后登录IP |
| login_fail_count | TINYINT UNSIGNED | NOT NULL | 0 | 连续登录失败次数 |
| locked_until | DATETIME | NULL | - | 账号锁定到期时间 |
| is_deleted | TINYINT UNSIGNED | NOT NULL | 0 | 是否删除：0=否，1=是 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `uk_username` (`username`)
- KEY: `idx_status` (`status`)
- KEY: `idx_is_deleted` (`is_deleted`)
- KEY: `idx_created_at` (`created_at`)

### 2.3 系统配置表 (system_configs)

存储系统配置信息。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| config_key | VARCHAR(100) | NOT NULL | - | 配置键 |
| config_value | TEXT | NULL | - | 配置值 |
| config_type | VARCHAR(20) | NOT NULL | 'string' | 配置类型：string/number/json/boolean |
| config_group | VARCHAR(50) | NULL | 'default' | 配置分组 |
| description | VARCHAR(500) | NULL | - | 配置说明 |
| version | INT UNSIGNED | NOT NULL | 1 | 配置版本号 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `uk_config_key` (`config_key`)
- KEY: `idx_config_group` (`config_group`)

### 2.4 会员表 (members)

存储微信小程序会员信息。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| openid | VARCHAR(64) | NOT NULL | - | 微信OpenID |
| nickname | VARCHAR(100) | NULL | - | 微信昵称 |
| avatar | VARCHAR(500) | NULL | - | 微信头像URL |
| phone | VARCHAR(20) | NULL | - | 手机号 |
| total_points | INT | NOT NULL | 0 | 当前积分总额 |
| is_deleted | TINYINT UNSIGNED | NOT NULL | 0 | 是否删除：0=否，1=是 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- UNIQUE KEY: `uk_openid` (`openid`)
- KEY: `idx_phone` (`phone`)
- KEY: `idx_is_deleted` (`is_deleted`)
- KEY: `idx_created_at` (`created_at`)

### 2.5 商品表 (products)

存储商品信息。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| name | VARCHAR(200) | NOT NULL | - | 商品名称 |
| image | VARCHAR(500) | NULL | - | 商品主图URL |
| detail_images | JSON | NULL | - | 商品详情图（JSON数组） |
| category | VARCHAR(50) | NULL | - | 商品分类 |
| type | TINYINT UNSIGNED | NOT NULL | 1 | 商品类型：1=全量商品，2=积分商品 |
| price | DECIMAL(10,2) | NULL | - | 商品价格（元） |
| points_price | INT | NULL | - | 积分价格 |
| description | TEXT | NULL | - | 商品描述 |
| status | TINYINT UNSIGNED | NOT NULL | 1 | 状态：1=上架，0=下架 |
| sort_order | INT | NOT NULL | 0 | 排序权重 |
| is_deleted | TINYINT UNSIGNED | NOT NULL | 0 | 是否删除：0=否，1=是 |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP | 更新时间 |

**索引**:
- PRIMARY KEY: `id`
- KEY: `idx_type_status_deleted` (`type`, `status`, `is_deleted`)
- KEY: `idx_category` (`category`)
- KEY: `idx_sort_order` (`sort_order`)

### 2.6 积分记录表 (points_records)

存储积分变动记录。

| 字段名 | 数据类型 | 是否为空 | 默认值 | 注释 |
|--------|----------|----------|--------|------|
| id | BIGINT UNSIGNED | NOT NULL | AUTO_INCREMENT | 主键ID |
| member_id | BIGINT UNSIGNED | NOT NULL | - | 会员ID |
| change_type | VARCHAR(50) | NOT NULL | - | 变动类型：消费赠送/积分兑换/后台调整/新会员注册/积分扣除/积分过期 |
| points | INT | NOT NULL | - | 变动分值（正数=增加，负数=扣除） |
| balance_before | INT | NOT NULL | - | 变动前积分余额 |
| balance_after | INT | NOT NULL | - | 变动后积分余额 |
| business_no | VARCHAR(64) | NULL | - | 业务单号（如订单号、兑换单号，便于对账） |
| product_id | BIGINT UNSIGNED | NULL | - | 关联商品ID（积分兑换时） |
| operator_type | VARCHAR(20) | NOT NULL | - | 操作人类型：admin/staff/system |
| operator_id | BIGINT UNSIGNED | NULL | - | 操作人ID（管理员或店员ID） |
| operator_name | VARCHAR(100) | NULL | - | 操作人姓名（冗余字段，便于查询） |
| remark | VARCHAR(500) | NULL | - | 备注/原因 |
| ip_address | VARCHAR(50) | NULL | - | 操作IP地址 |
| user_agent | VARCHAR(500) | NULL | - | 用户代理（设备信息） |
| created_at | DATETIME | NOT NULL | CURRENT_TIMESTAMP | 创建时间 |

**索引**:
- PRIMARY KEY: `id`
- KEY: `idx_member_created` (`member_id`, `created_at` DESC)
- KEY: `idx_operator_created` (`operator_type`, `operator_id`, `created_at` DESC)
- KEY: `idx_created_at` (`created_at`)
- KEY: `idx_change_type` (`change_type`)
- KEY: `idx_business_no` (`business_no`)
- KEY: `idx_product_id` (`product_id`)
- FOREIGN KEY: `fk_points_records_member` (`member_id`) REFERENCES `members` (`id`) ON DELETE RESTRICT
- FOREIGN KEY: `fk_points_records_product` (`product_id`) REFERENCES `products` (`id`) ON DELETE SET NULL

## 3. 数据库视图

### 3.1 会员积分统计视图 (v_member_points_stats)

用于统计会员积分相关信息。

```sql
CREATE VIEW `v_member_points_stats` AS
SELECT  
    m.id,
    m.nickname,
    m.phone,
    m.total_points AS current_points,
    COALESCE(SUM(CASE WHEN pr.points > 0 THEN pr.points ELSE 0 END), 0) AS total_earned,
    COALESCE(SUM(CASE WHEN pr.points < 0 THEN ABS(pr.points) ELSE 0 END), 0) AS total_spent,
    COUNT(pr.id) AS record_count,
    MAX(pr.created_at) AS last_points_time
FROM members m
LEFT JOIN points_records pr ON m.id = pr.member_id
WHERE m.is_deleted = 0
GROUP BY m.id, m.nickname, m.phone, m.total_points;
```

### 3.2 数据大屏统计视图 (v_dashboard_stats)

用于数据大屏统计。

```sql
CREATE VIEW `v_dashboard_stats` AS
SELECT  
    (SELECT COUNT(*) FROM members WHERE is_deleted = 0) AS total_members,
    (SELECT COALESCE(SUM(points), 0) FROM points_records WHERE points > 0) AS total_points_issued,
    (SELECT COALESCE(SUM(ABS(points)), 0) FROM points_records WHERE points < 0) AS total_points_consumed,
    (SELECT COALESCE(SUM(total_points), 0) FROM members WHERE is_deleted = 0) AS current_points_pool;
```

## 4. 存储过程

### 4.1 积分一致性检查存储过程 (sp_check_points_consistency)

用于检查会员积分总额与积分记录是否一致。

```sql
CREATE PROCEDURE `sp_check_points_consistency`()
BEGIN
    -- 检查会员积分总额与积分记录是否一致
    SELECT
        m.id AS member_id,
        m.nickname,
        m.total_points AS db_points,
        COALESCE(MAX(pr.balance_after), 0) AS calculated_points,
        CASE 
            WHEN m.total_points = COALESCE(MAX(pr.balance_after), 0) THEN '一致'
            ELSE '不一致'
        END AS status
    FROM members m
    LEFT JOIN points_records pr ON m.id = pr.member_id
    WHERE m.is_deleted = 0
    GROUP BY m.id, m.nickname, m.total_points
    HAVING m.total_points != COALESCE(MAX(pr.balance_after), 0);
END;
```

## 5. 初始化数据

### 5.1 系统配置初始数据

```sql
INSERT INTO `system_configs` (`config_key`, `config_value`, `config_type`, `config_group`, `description`) 
VALUES 
('wechat_qrcode', '', 'string', 'wechat', '企业微信咨询二维码URL'),
('points_register_gift', '100', 'number', 'points', '新会员注册赠送积分'),
('points_consumption_ratio', '0.01', 'number', 'points', '消费积分比例（1元=0.01积分）')
ON DUPLICATE KEY UPDATE 
  `description` = VALUES(`description`),
  `config_type` = VALUES(`config_type`),
  `config_group` = VALUES(`config_group`);
```

### 5.2 示例商品数据

```sql
INSERT INTO `products` (`name`, `image`, `category`, `type`, `price`, `points_price`, `status`, `sort_order`)
VALUES
('品牌8K超高清智能电视75英寸', 'https://example.com/tv.jpg', '电视', 1, 8999.00, NULL, 1, 100),
('智能变频对开门冰箱580L', 'https://example.com/fridge.jpg', '冰箱', 1, 5999.00, NULL, 1, 90),
('全自动滚筒洗衣机10kg', 'https://example.com/washer.jpg', '洗衣机', 1, 3999.00, NULL, 1, 80),
('积分商品-精美礼品盒', 'https://example.com/gift.jpg', '礼品', 2, NULL, 500, 1, 70),
('积分商品-定制马克杯', 'https://example.com/mug.jpg', '礼品', 2, NULL, 200, 1, 60);
```

## 6. 业务逻辑说明

### 6.1 积分变动类型说明
- `消费赠送`: 客户购买商品后赠送积分
- `积分兑换`: 使用积分兑换商品
- `后台调整`: 管理员或店员后台调整积分
- `新会员注册`: 新会员注册赠送积分
- `积分扣除`: 积分扣除操作
- `积分过期`: 积分过期失效

### 6.2 用户角色说明
- `admin`: 管理员，拥有所有权限
- `staff`: 店员，拥有部分权限
- `system`: 系统操作

### 6.3 商品类型说明
- `1`: 全量商品（普通商品）
- `2`: 积分商品（积分兑换商品）