# 华康电器连锁积分小程序 - 建表逻辑总结

## 🎯 核心建表逻辑（一句话总结）

**按照业务依赖关系，先建基础表（管理员、店员、配置），再建业务表（会员、商品），最后建关联表（积分记录），并确保积分数据一致性。**

---

## 📊 表结构设计逻辑

### 一、表分类（按依赖关系）

```
第一层：基础表（无外键依赖）
├── admins（管理员表）
├── staffs（店员表）
└── system_configs（系统配置表）

第二层：业务核心表（无外键依赖）
├── members（会员表）
└── products（商品表）

第三层：关联表（有外键依赖）
└── points_records（积分记录表）
    ├── 依赖 members（会员ID）
    └── 依赖 products（商品ID，可选）
```

---

## 🔑 核心设计逻辑

### 1. **会员表 (members) 设计逻辑**

**为什么这样设计？**
- `openid` 唯一：微信用户唯一标识，用于登录识别
- `phone` 唯一：业务要求，一个手机号只能绑定一个会员
- `total_points`：当前积分总额，**必须与积分记录表保持一致**
- `is_deleted` 软删除：删除会员不物理删除，保留历史数据

**关键约束：**
```sql
UNIQUE KEY uk_openid (openid)  -- 微信登录必须唯一
UNIQUE KEY uk_phone (phone)    -- 手机号唯一（业务规则）
```

---

### 2. **商品表 (products) 设计逻辑**

**为什么这样设计？**
- **一张表存储两种商品类型**：通过 `type` 字段区分（1=全量商品，2=积分商品）
- **价格字段设计**：
  - 全量商品：`price` 必填，`points_price` 为空
  - 积分商品：`points_price` 必填，`price` 可为空（显示原价）
- `is_deleted` 软删除：下架商品不删除，保留历史数据

**业务规则（应用层保证）：**
```sql
-- 全量商品：type=1，price 必填，points_price 为空
-- 积分商品：type=2，points_price 必填，price 可为空
```

**索引设计：**
```sql
KEY idx_type_status_deleted (type, status, is_deleted)
-- 查询上架商品列表时使用此索引
```

---

### 3. **积分记录表 (points_records) 设计逻辑** ⚠️ **最重要**

**为什么这样设计？**

#### 3.1 核心字段设计

1. **余额快照字段（审计必须）**
   ```sql
   balance_before INT  -- 变动前余额
   balance_after INT   -- 变动后余额
   ```
   **作用：** 便于对账、审计、追溯。可以验证：`balance_after = balance_before + points`

2. **操作人信息（权限控制）**
   ```sql
   operator_type VARCHAR(20)  -- admin/staff/system
   operator_id BIGINT         -- 操作人ID
   operator_name VARCHAR(100) -- 操作人姓名（冗余，便于查询）
   ```
   **作用：** 
   - 管理员可以查看所有记录
   - 店员只能查看 `operator_type='staff' AND operator_id=当前店员ID` 的记录

3. **业务单号（对账必须）**
   ```sql
   business_no VARCHAR(64) UNIQUE
   ```
   **作用：** 关联订单、兑换单等业务单据，便于对账

4. **审计字段（企业级必须）**
   ```sql
   ip_address VARCHAR(50)   -- 操作IP
   user_agent VARCHAR(500)   -- 设备信息
   ```
   **作用：** 记录操作环境，便于追溯问题

#### 3.2 数据一致性保证

**问题：** 会员表的 `total_points` 必须等于该会员最后一条积分记录的 `balance_after`

**解决方案：**

**方案一：应用层事务（推荐）**
```sql
BEGIN;
  -- 1. 查询当前余额
  SELECT total_points INTO @balance_before FROM members WHERE id = ?;
  
  -- 2. 计算新余额
  SET @balance_after = @balance_before + ?;
  
  -- 3. 插入积分记录（包含余额快照）
  INSERT INTO points_records (member_id, points, balance_before, balance_after, ...)
  VALUES (?, ?, @balance_before, @balance_after, ...);
  
  -- 4. 更新会员积分
  UPDATE members SET total_points = @balance_after WHERE id = ?;
COMMIT;
```

**方案二：定时对账（必须）**
```sql
-- 每天凌晨执行存储过程检查
CALL sp_check_points_consistency();
-- 发现不一致时告警，人工修复
```

---

### 4. **店员表 (staffs) 和管理员表 (admins) 设计逻辑**

**为什么分开设计？**
- 权限不同：管理员有全部权限，店员只有会员管理权限
- 业务独立：店员由管理员创建，两个表业务逻辑不同
- **未来扩展：** 如果未来需要统一管理，可以合并为 `users` 表 + `role` 字段

**安全设计：**
```sql
password_hash VARCHAR(255)      -- BCrypt加密
login_fail_count TINYINT        -- 登录失败次数
locked_until DATETIME          -- 账号锁定时间（防暴力破解）
```

---

### 5. **系统配置表 (system_configs) 设计逻辑**

**为什么这样设计？**
- `config_key` 唯一：配置键必须唯一
- `config_group` 分组：便于管理（如 wechat、points 等分组）
- `config_type` 类型：支持 string、number、json、boolean 等类型
- `version` 版本：支持配置回滚（可选）

---

## 🔄 数据流转逻辑

### 积分变动流程

```
1. 用户操作（消费/兑换/调整）
   ↓
2. 应用层开始事务
   ↓
3. 查询会员当前积分（balance_before）
   ↓
4. 计算新积分（balance_after = balance_before + points）
   ↓
5. 插入积分记录（包含余额快照）
   ↓
6. 更新会员表 total_points
   ↓
7. 提交事务
```

**关键点：**
- ✅ 必须使用事务，保证原子性
- ✅ 余额快照必须准确
- ✅ 操作人信息必须完整

---

## 📈 索引设计逻辑

### 索引选择原则

1. **高频查询字段必须建索引**
   - 会员表：`openid`（登录）、`phone`（查询）
   - 商品表：`(type, status, is_deleted)`（商品列表）

2. **复合索引优化多条件查询**
   - 积分记录：`(member_id, created_at DESC)`（会员积分历史）
   - 积分记录：`(operator_type, operator_id, created_at DESC)`（店员查询）

3. **避免过度索引**
   - 不常用的查询条件不建索引
   - 区分度低的字段不建索引（如 status 只有 0/1）

---

## ⚠️ 企业级注意事项

### 1. 积分数据一致性（最重要）

**问题：** 会员表的 `total_points` 与积分记录表不一致

**原因：**
- 并发操作导致
- 程序bug导致
- 手动修改数据库导致

**解决方案：**
- ✅ 应用层使用事务
- ✅ 定时对账检查（每天凌晨）
- ✅ 发现不一致时告警，人工修复

### 2. 软删除设计

**为什么用软删除？**
- 保留历史数据，便于审计
- 积分记录表需要关联会员，不能物理删除
- 商品下架后，积分记录中的商品ID需要保留

**实现：**
```sql
is_deleted TINYINT DEFAULT 0  -- 0=未删除，1=已删除
-- 所有查询都需要加上 WHERE is_deleted = 0
```

### 3. 权限控制实现

**店员只能查看自己的操作：**
```sql
-- 店员查询积分记录
SELECT * FROM points_records 
WHERE operator_type = 'staff' 
  AND operator_id = ?  -- 当前店员ID
  AND created_at >= ? AND created_at <= ?
ORDER BY created_at DESC;
```

**管理员可以查看所有：**
```sql
-- 管理员查询积分记录
SELECT * FROM points_records 
WHERE created_at >= ? AND created_at <= ?
ORDER BY created_at DESC;
```

---

## 📋 建表顺序（必须按此顺序）

```
1. admins（管理员表）
2. staffs（店员表）
3. system_configs（系统配置表）
4. members（会员表）
5. products（商品表）
6. points_records（积分记录表，依赖 members 和 products）
```

**原因：** 外键约束要求被引用的表必须先创建

---

## 🎯 总结

### 核心设计理念

1. **数据完整性优先**：外键约束、唯一性约束、非空约束
2. **审计追踪必须**：时间戳、操作人、余额快照、IP地址
3. **数据一致性保证**：事务 + 对账双重保障
4. **性能优化合理**：索引覆盖高频查询，避免过度索引
5. **扩展性预留**：软删除、预留字段、配置表

### 最关键的三点

1. ⚠️ **积分记录表是核心**，设计必须严谨，包含余额快照
2. ⚠️ **积分总额一致性**必须通过事务+对账双重保证
3. ⚠️ **店员权限**通过 `operator_type` + `operator_id` 实现数据隔离

---

## 📝 使用建议

1. **开发环境：** 使用 `database_init_enterprise.sql` 初始化数据库
2. **生产环境：** 
   - 先执行建表脚本
   - 再执行对账存储过程测试
   - 配置定时任务每天对账
3. **数据迁移：** 如果已有旧数据，需要编写迁移脚本，确保积分一致性
