# 积分记录查询接口优化说明

**修改时间：** 2026-01-30  
**修改类型：** 功能优化  
**影响接口：** GET /api/admin/points-records/list

---

## 📝 修改内容

### 修改前

查询条件使用精确的ID匹配：
- `memberId` - 会员ID（精确匹配）
- `operatorType` - 操作人类型（精确匹配）
- `operatorId` - 操作人ID（精确匹配）
- `startTime` - 开始时间
- `endTime` - 结束时间

### 修改后

查询条件改为关键字模糊搜索：
- `memberKeyword` - 会员关键字（支持姓名/昵称/手机号模糊搜索）
- `operatorKeyword` - 操作人关键字（支持姓名模糊搜索）
- `startTime` - 开始时间（保持不变）
- `endTime` - 结束时间（保持不变）

**数据权限保持不变：** 店员只能查询自己操作的记录

---

## 🔧 修改的文件

### 1. PointsRecordListDTO.java

**文件路径：** `backend/huakang-service/src/main/java/com/huakang/service/dto/points/PointsRecordListDTO.java`

**修改内容：**

```java
// 修改前
private Long memberId;
private String operatorType;
private Long operatorId;

// 修改后
@Schema(description = "会员关键字（支持姓名/昵称/手机号模糊搜索）", example = "张三")
private String memberKeyword;

@Schema(description = "操作人关键字（支持姓名模糊搜索）", example = "李四")
private String operatorKeyword;
```

### 2. PointsRecordServiceImpl.java

**文件路径：** `backend/huakang-service/src/main/java/com/huakang/service/impl/PointsRecordServiceImpl.java`

**修改内容：**

```java
// 按会员关键字筛选（姓名/昵称/手机号）
if (queryDTO.getMemberKeyword() != null && !queryDTO.getMemberKeyword().trim().isEmpty()) {
    String keyword = queryDTO.getMemberKeyword().trim();
    // 先查询符合条件的会员ID列表
    LambdaQueryWrapper<Member> memberWrapper = new LambdaQueryWrapper<>();
    memberWrapper.and(w -> w
            .like(Member::getName, keyword)
            .or().like(Member::getNickname, keyword)
            .or().like(Member::getPhone, keyword)
    );
    List<Member> members = memberMapper.selectList(memberWrapper);
    
    if (members.isEmpty()) {
        // 如果没有匹配的会员，返回空结果
        wrapper.eq(PointsRecord::getMemberId, -1L);
    } else {
        // 使用会员ID列表进行筛选
        List<Long> memberIds = members.stream()
                .map(Member::getId)
                .toList();
        wrapper.in(PointsRecord::getMemberId, memberIds);
    }
}

// 按操作人关键字筛选（操作人姓名）
if (queryDTO.getOperatorKeyword() != null && !queryDTO.getOperatorKeyword().trim().isEmpty()) {
    String keyword = queryDTO.getOperatorKeyword().trim();
    wrapper.like(PointsRecord::getOperatorName, keyword);
}
```

### 3. PointsRecordController.java

**文件路径：** `backend/huakang-admin/src/main/java/com/huakang/admin/controller/PointsRecordController.java`

**修改内容：** 更新接口描述

```java
@Operation(summary = "积分记录列表", 
    description = "分页查询积分记录，支持按会员关键字（姓名/昵称/手机号）、操作人关键字（姓名）、时间范围筛选。店员只能查看自己的记录")
```

---

## 📊 API接口变化

### 请求参数

**接口地址：** `GET /api/admin/points-records/list`

**请求参数对比：**

| 参数名 | 修改前 | 修改后 | 说明 |
|--------|--------|--------|------|
| page | Integer | Integer | 页码（不变） |
| size | Integer | Integer | 每页大小（不变） |
| memberId | Long | ❌ 删除 | 会员ID |
| operatorType | String | ❌ 删除 | 操作人类型 |
| operatorId | Long | ❌ 删除 | 操作人ID |
| memberKeyword | ❌ 不存在 | String | ✅ 新增：会员关键字 |
| operatorKeyword | ❌ 不存在 | String | ✅ 新增：操作人关键字 |
| startTime | LocalDateTime | LocalDateTime | 开始时间（不变） |
| endTime | LocalDateTime | LocalDateTime | 结束时间（不变） |

### 请求示例

#### 修改前

```bash
GET /api/admin/points-records/list?page=1&size=10&memberId=1&operatorId=2&startTime=2025-01-01T00:00:00&endTime=2025-01-31T23:59:59
```

#### 修改后

```bash
GET /api/admin/points-records/list?page=1&size=10&memberKeyword=张三&operatorKeyword=李四&startTime=2025-01-01T00:00:00&endTime=2025-01-31T23:59:59
```

### 响应格式

响应格式保持不变：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 100,
    "pages": 10,
    "current": 1,
    "size": 10,
    "records": [
      {
        "id": 1,
        "memberId": 1,
        "memberNickname": "张三",
        "memberPhone": "13800138000",
        "changeType": "消费赠送",
        "points": 100,
        "balanceBefore": 0,
        "balanceAfter": 100,
        "operatorType": "staff",
        "operatorName": "李四",
        "createdAt": "2025-01-15T10:30:00",
        "remark": "购买商品赠送积分"
      }
    ]
  }
}
```

---

## 🎯 功能特性

### 1. 会员关键字搜索

支持以下字段的模糊搜索：
- **姓名** - 会员真实姓名
- **昵称** - 会员昵称
- **手机号** - 会员手机号

**搜索逻辑：** OR关系（任一字段匹配即可）

**示例：**
- 搜索 "张" - 匹配姓名或昵称包含"张"的会员
- 搜索 "138" - 匹配手机号包含"138"的会员

### 2. 操作人关键字搜索

支持操作人姓名的模糊搜索：
- **操作人姓名** - 存储在 `operator_name` 字段

**搜索逻辑：** LIKE模糊匹配

**示例：**
- 搜索 "李" - 匹配操作人姓名包含"李"的记录

### 3. 数据权限控制

**管理员（admin）：**
- 可以查看所有积分记录
- 可以使用任意关键字搜索

**店员（staff）：**
- 只能查看自己操作的记录
- 即使输入其他操作人关键字，也只能看到自己的记录
- 可以搜索任意会员的记录（但仅限自己操作的）

---

## 🔍 SQL查询优化

### 会员关键字搜索

```sql
-- 第一步：查询符合条件的会员
SELECT id FROM members 
WHERE name LIKE '%张三%' 
   OR nickname LIKE '%张三%' 
   OR phone LIKE '%张三%';

-- 第二步：使用会员ID列表查询积分记录
SELECT * FROM points_records 
WHERE member_id IN (1, 2, 3, ...)
ORDER BY created_at DESC;
```

### 操作人关键字搜索

```sql
-- 直接在积分记录表中搜索
SELECT * FROM points_records 
WHERE operator_name LIKE '%李四%'
ORDER BY created_at DESC;
```

---

## ✅ 测试用例

### 测试1：会员姓名搜索

**请求：**
```bash
GET /api/admin/points-records/list?memberKeyword=张三
```

**预期结果：** 返回所有会员姓名、昵称或手机号包含"张三"的积分记录

### 测试2：操作人姓名搜索

**请求：**
```bash
GET /api/admin/points-records/list?operatorKeyword=李四
```

**预期结果：** 返回所有操作人姓名包含"李四"的积分记录

### 测试3：组合搜索

**请求：**
```bash
GET /api/admin/points-records/list?memberKeyword=张三&operatorKeyword=李四&startTime=2025-01-01T00:00:00&endTime=2025-01-31T23:59:59
```

**预期结果：** 返回符合所有条件的积分记录

### 测试4：店员权限测试

**场景：** 店员A登录，搜索其他操作人

**请求：**
```bash
GET /api/admin/points-records/list?operatorKeyword=店员B
```

**预期结果：** 只返回店员A自己操作的记录，不会返回店员B的记录

### 测试5：空结果测试

**请求：**
```bash
GET /api/admin/points-records/list?memberKeyword=不存在的会员
```

**预期结果：** 返回空列表

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "total": 0,
    "pages": 0,
    "current": 1,
    "size": 10,
    "records": []
  }
}
```

---

## 📝 前端调整建议

### 1. 修改查询表单

```html
<!-- 修改前 -->
<el-form-item label="会员ID">
  <el-input v-model="queryForm.memberId" placeholder="请输入会员ID" />
</el-form-item>
<el-form-item label="操作人ID">
  <el-input v-model="queryForm.operatorId" placeholder="请输入操作人ID" />
</el-form-item>

<!-- 修改后 -->
<el-form-item label="会员关键字">
  <el-input v-model="queryForm.memberKeyword" placeholder="姓名/昵称/手机号" />
</el-form-item>
<el-form-item label="操作人">
  <el-input v-model="queryForm.operatorKeyword" placeholder="操作人姓名" />
</el-form-item>
```

### 2. 修改请求参数

```javascript
// 修改前
const queryForm = {
  page: 1,
  size: 10,
  memberId: null,
  operatorId: null,
  startTime: null,
  endTime: null
}

// 修改后
const queryForm = {
  page: 1,
  size: 10,
  memberKeyword: '',
  operatorKeyword: '',
  startTime: null,
  endTime: null
}
```

---

## 🎊 优化效果

### 用户体验提升

1. **更灵活的搜索** - 不需要记住会员ID，可以直接搜索姓名或手机号
2. **更快的查询** - 不需要先查会员列表再复制ID
3. **更友好的界面** - 输入框提示更清晰

### 性能影响

1. **会员关键字搜索** - 需要先查询会员表，然后再查询积分记录表（两次查询）
2. **操作人关键字搜索** - 直接在积分记录表中搜索（一次查询）
3. **建议添加索引** - 在 `members` 表的 `name`、`nickname`、`phone` 字段上添加索引

---

## 🔄 数据库索引建议

为了优化搜索性能，建议添加以下索引：

```sql
-- 会员表索引
CREATE INDEX idx_member_name ON members(name);
CREATE INDEX idx_member_nickname ON members(nickname);
CREATE INDEX idx_member_phone ON members(phone);

-- 积分记录表索引
CREATE INDEX idx_points_record_operator_name ON points_records(operator_name);
```

---

## ✅ 修改完成

- [x] 修改 PointsRecordListDTO.java
- [x] 修改 PointsRecordServiceImpl.java
- [x] 修改 PointsRecordController.java
- [x] 添加Swagger注解
- [x] 编写修改说明文档

---

**修改完成时间：** 2026-01-30  
**修改人：** AI Assistant  
**状态：** ✅ 已完成，待测试验证
