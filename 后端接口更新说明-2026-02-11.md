# 后端接口更新说明

**更新日期：** 2026-02-7  
**版本：** v1.2.0

---

## 📋 本次更新概览

根据会议需求，本次更新主要涉及以下功能：

1. ✅ **新增下架商品查询接口**
2. ✅ **会员积分记录增加关联商品信息**
3. ✅ **导出报表增加关联商品列**
4. ✅ **商品批量排序接口**
5. ⚠️ **待实现：视频上传功能（需评估）**

---

## 1️⃣ 新增接口

### 1.1 查询下架商品列表

**接口地址：** `GET /products/offline`

**权限要求：** 仅管理员

**请求参数：** 与商品列表接口相同，支持分页和筛选

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | Integer | 否 | 页码，默认1 | 1 |
| size | Integer | 否 | 每页大小，默认10 | 10 |
| type | Integer | 否 | 商品类型：1=全量商品，2=积分商品 | 1 |
| category | String | 否 | 商品分类 | 家用电器 |
| name | String | 否 | 商品名称（模糊查询） | 冰箱 |

**响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 15,
    "pages": 2,
    "current": 1,
    "size": 10,
    "records": [
      {
        "id": 1,
        "name": "海尔冰箱",
        "image": "https://xxx.com/image.jpg",
        "category": "家用电器",
        "type": 1,
        "price": 2999.00,
        "status": 0,
        "sortOrder": 100,
        "createdAt": "2025-01-01T10:00:00",
        "updatedAt": "2025-01-15T14:30:00"
      }
    ]
  }
}
```

---

### 1.2 批量更新商品排序

**接口地址：** `PUT /products/batch-sort`

**权限要求：** 仅管理员

**请求参数：**
```json
{
  "items": [
    { "id": 1, "sortOrder": 100 },
    { "id": 2, "sortOrder": 90 },
    { "id": 3, "sortOrder": 80 }
  ]
}
```

**参数说明：**

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| items | Array | 是 | 排序项列表 |
| items[].id | Long | 是 | 商品ID |
| items[].sortOrder | Integer | 是 | 排序权重（数值越大越靠前） |

**响应示例：**
```json
{
  "code": 200,
  "message": "排序更新成功",
  "data": null
}
```

**前端建议：**
- 使用拖拽库（如 `react-beautiful-dnd` 或 `@dnd-kit/core`）实现拖拽排序
- 拖拽完成后，根据新顺序重新计算 `sortOrder` 值（建议按100、90、80...递减）
- 调用此接口批量更新排序

---

## 2️⃣ 修改接口

### 2.1 积分记录列表（新增字段）

**接口地址：** `GET /points-records/list`

**变更说明：** 返回数据新增 `productId` 和 `productName` 字段

**新增响应字段：**

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| productId | Long | 关联商品ID（可能为null） | 1 |
| productName | String | 关联商品名称（可能为null） | 海尔冰箱 |

**完整响应示例：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 100,
    "records": [
      {
        "id": 1,
        "memberId": 10,
        "memberNickname": "张三",
        "memberPhone": "13800138000",
        "changeType": "积分兑换",
        "points": -500,
        "balanceBefore": 1000,
        "balanceAfter": 500,
        "operatorType": "system",
        "operatorName": "系统",
        "createdAt": "2025-01-01T10:00:00",
        "remark": "兑换商品",
        "productId": 1,
        "productName": "海尔冰箱"
      }
    ]
  }
}
```

**前端展示建议：**
- 在会员积分记录列表中新增"关联电器"列
- 如果 `productName` 为 null，显示为 "-" 或空

---

### 2.2 导出积分记录（新增列）

**接口地址：** `GET /export/points-records`

**变更说明：** 导出的Excel文件新增"关联商品"列

**Excel列顺序：**

| 列序号 | 列名 | 说明 |
|--------|------|------|
| 0 | 记录ID | - |
| 1 | 会员昵称 | - |
| 2 | 会员手机号 | - |
| 3 | 变动类型 | - |
| 4 | 变动分值 | - |
| 5 | 变动前余额 | - |
| 6 | 变动后余额 | - |
| 7 | 操作人类型 | - |
| 8 | 操作人姓名 | - |
| 9 | 业务单号 | - |
| 10 | 备注 | - |
| 11 | **关联商品** | **新增列** |
| 12 | 操作时间 | - |

**前端建议：**
- 导出前可增加文件命名弹窗，让用户自定义文件名
- 后端默认文件名格式：`积分记录_开始日期_结束日期.xlsx`

---

## 3️⃣ 现有功能确认

### 3.1 商品排序功能

**当前状态：** ✅ 已支持

- 商品实体已有 `sortOrder` 字段（排序权重）
- 创建/更新商品时可设置 `sortOrder`
- 列表查询按 `sortOrder DESC` 排序（数值越大越靠前）

**前端实现建议：**
1. 使用拖拽库（如 `react-beautiful-dnd` 或 `@dnd-kit/core`）
2. 拖拽后计算新的 `sortOrder` 值
3. 调用 `PUT /products/{id}` 接口更新 `sortOrder`

**批量更新排序接口：** ✅ 已实现
```
PUT /products/batch-sort
Body: {
  "items": [
    { "id": 1, "sortOrder": 100 },
    { "id": 2, "sortOrder": 90 },
    { "id": 3, "sortOrder": 80 }
  ]
}
```

---

### 3.2 图片上传功能

**当前状态：** ✅ 已支持

**接口地址：** `POST /products/upload`

**请求参数：**
- `file`: MultipartFile（图片文件）

**响应示例：**
```json
{
  "code": 200,
  "message": "上传成功",
  "data": "https://oss.example.com/products/xxx.jpg"
}
```

**支持的图片格式：** JPG、PNG、GIF、WEBP

---

### 3.3 视频上传功能

**当前状态：** ❌ 未实现

**需求分析：**
- 商品表目前只有 `image`（主图）和 `detailImages`（详情图）字段
- 如需支持视频，需要：
  1. 数据库增加 `video_url` 字段
  2. 后端增加视频上传接口（OSS配置）
  3. 前端增加视频播放器组件

**建议方案：**
1. **短期方案：** 将视频URL作为详情图的一部分存储（前端判断URL后缀播放视频）
2. **长期方案：** 新增视频字段，支持多视频上传

---

## 4️⃣ 待实现功能清单

### 后端待实现

| 功能 | 优先级 | 预计工时 |
|------|--------|----------|
| 视频上传接口（如需要） | 低 | 2小时 |
| 数据库增加视频字段（如需要） | 低 | 0.5小时 |

### 前端待实现

| 功能 | 优先级 |
|------|--------|
| 商品拖拽排序UI | 高 |
| 积分记录列表增加"关联电器"列 | 高 |
| 导出文件命名弹窗 | 中 |
| 小程序首页悬浮窗 | 高 |
| 视频上传组件（如需要） | 低 |

---

## 5️⃣ 数据库变更

**本次更新：** 无需修改数据库

**说明：**
- `points_records` 表已有 `product_id` 字段，可直接使用
- `products` 表已有 `sort_order` 字段，支持排序功能

**如需支持视频，建议执行以下SQL：**
```sql
ALTER TABLE products 
ADD COLUMN video_url VARCHAR(500) COMMENT '商品视频URL' AFTER detail_images;
```

---

## 6️⃣ 测试建议

### 6.1 下架商品接口测试
```bash
# 查询下架商品
GET http://localhost:8080/products/offline?page=1&size=10

# 预期：只返回 status=0 的商品
```

### 6.2 积分记录关联商品测试
```bash
# 查询积分记录
GET http://localhost:8080/points-records/list?page=1&size=10

# 检查返回数据是否包含 productId 和 productName 字段
```

### 6.3 导出报表测试
```bash
# 导出积分记录
GET http://localhost:8080/export/points-records?startTime=2025-01-01 00:00:00&endTime=2025-12-31 23:59:59

# 检查Excel文件是否包含"关联商品"列（第12列）
```

---

## 7️⃣ 注意事项

1. **性能优化：** 积分记录查询和导出时，商品信息采用批量查询，避免N+1问题
2. **兼容性：** 旧数据中 `product_id` 可能为 null，前端需做空值处理
3. **权限控制：** 下架商品接口仅管理员可访问
4. **文件命名：** 导出文件名支持中文，已做URL编码处理

---

## 8️⃣ 会议需求对比

### ✅ 已完成功能

1. **商品管理功能**
   - ✅ 图片上传功能（已支持）
   - ✅ 拖拽排序后端支持（批量更新排序接口已实现）
   - ⚠️ 视频上传功能（待评估，建议短期将视频URL存入detailImages）

2. **会员积分记录功能**
   - ✅ 导出功能（已支持，建议前端增加文件命名弹窗）
   - ✅ 记录列表增加"关联电器"列（已实现productId和productName字段）

3. **小程序悬浮窗设计**
   - ⚠️ 纯前端功能，无需后端支持

### 📋 实施建议

**后端已完成：**
- 下架商品查询接口
- 积分记录关联商品信息
- 导出报表增加商品列
- 批量更新排序接口

**前端待实现：**
- 商品拖拽排序UI（使用react-beautiful-dnd或@dnd-kit/core）
- 积分记录列表增加"关联电器"列显示
- 导出文件命名弹窗
- 小程序首页悬浮窗

**待评估功能：**
- 视频上传（需确认是否必需，如需要则需2.5小时开发）

---

## 9️⃣ 技术实现说明

### 批量排序实现原理
- 前端拖拽后计算新的sortOrder值
- 调用批量更新接口一次性更新多个商品
- 后端清除缓存，确保数据一致性

### 关联商品查询优化
- 采用批量查询避免N+1问题
- 使用Map缓存商品信息
- 空值安全处理，兼容旧数据

### 导出功能增强
- Excel新增"关联商品"列
- 支持中文文件名（URL编码）
- 默认文件名格式：积分记录_开始日期_结束日期.xlsx

---

**文档版本：** v1.0  
**最后更新：** 2026-02-11
