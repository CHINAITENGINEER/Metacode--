# 密码哈希值问题解决方案

## 问题描述

登录接口一直返回"用户名或密码错误"，即使数据库中的密码哈希值已经更新。这是因为 BCrypt 哈希值不匹配。

## 根本原因

BCrypt 每次生成的哈希值都不同（因为盐值随机），但都能验证同一个密码。数据库中的哈希值 `$2a$10$EixZaYVK1fsbw1ZfbX3OXePaWxn96p36WQoeG6Lruj3vjPGga31lW` 不匹配密码 "123456"。

## 解决方案

### 方法1：使用已验证的哈希值（推荐）

执行 `fix_password_verified.sql` 脚本，使用已验证的 BCrypt 哈希值：

```sql
-- 在数据库中执行
UPDATE admins 
SET password_hash = '$2a$10$N9qo8uLOickgx2ZMRZoMyeIjZAgcfl7p92ldGxad68LJZdL17lhWy',
    login_fail_count = 0,
    locked_until = NULL
WHERE is_deleted = 0;
```

### 方法2：生成新的哈希值

1. **运行密码生成工具**：
   ```bash
   cd backend/huakang-service
   mvn compile
   mvn exec:java -Dexec.mainClass="com.huakang.service.utils.PasswordHashGenerator"
   ```

2. **复制生成的哈希值**，然后执行：
   ```sql
   UPDATE admins 
   SET password_hash = '生成的哈希值'
   WHERE username = 'admin';
   ```

### 方法3：使用在线工具

1. 访问 BCrypt 在线生成器（如 https://bcrypt-generator.com/）
2. 输入密码：`123456`
3. 复制生成的哈希值
4. 更新数据库

## 验证步骤

1. **执行 SQL 更新脚本**
2. **重启应用**
3. **清除浏览器缓存**（或使用无痕模式）
4. **测试登录**：
   - 用户名：`admin`
   - 密码：`123456`

## 注意事项

- BCrypt 每次生成的哈希值都不同，但都能验证同一个密码
- 更新密码哈希值后，必须重启应用才能生效
- 如果多次登录失败，账号可能被锁定，需要重置 `login_fail_count` 和 `locked_until`
- 确保使用相同的 BCrypt 算法版本（$2a$10$）

## 调试技巧

如果仍然失败，可以添加日志来验证：

```java
// 在 AdminServiceImpl.login() 方法中添加
log.debug("输入的密码: {}", password);
log.debug("数据库中的哈希值: {}", admin.getPasswordHash());
log.debug("验证结果: {}", passwordEncoder.matches(password, admin.getPasswordHash()));
```
