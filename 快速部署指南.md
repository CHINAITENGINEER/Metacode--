# 华康电器连锁积分小程序 - 快速部署指南

## 🚀 快速开始

### 前置条件

- Docker 20.10+
- Docker Compose 2.0+
- 服务器内存 4GB+
- 服务器硬盘 40GB+

### 一、生成安全密钥（必须）

```bash
# 1. 生成JWT密钥（64位）
openssl rand -base64 64

# 2. 生成Redis密码（32位）
openssl rand -base64 32

# 3. 生成数据库密码（24位）
openssl rand -base64 24
```

### 二、配置环境变量

```bash
# 1. 复制配置模板
cp env.prod.example .env.prod

# 2. 编辑配置文件
nano .env.prod

# 3. 填写以下必填项：
# - DB_ROOT_PASSWORD（数据库root密码）
# - DB_PASSWORD（应用数据库密码）
# - REDIS_PASSWORD（Redis密码）
# - JWT_SECRET（JWT密钥，使用上面生成的）
# - OSS_ACCESS_KEY_ID（阿里云OSS）
# - OSS_ACCESS_KEY_SECRET（阿里云OSS）
# - OSS_BUCKET_NAME（阿里云OSS）
# - OSS_DOMAIN（阿里云OSS）
# - WECHAT_APPID（微信小程序AppID）
# - WECHAT_SECRET（微信小程序Secret）
```

### 三、一键部署

```bash
# 1. 赋予执行权限
chmod +x deploy.sh

# 2. 执行部署脚本
./deploy.sh prod

# 3. 查看服务状态
docker-compose -f docker-compose.prod.yml ps

# 4. 查看日志
docker-compose -f docker-compose.prod.yml logs -f
```

### 四、验证部署

```bash
# 1. 检查Admin服务
curl http://localhost:8080/api/admin/health/ping

# 2. 检查Miniapp服务
curl http://localhost:8081/api/miniapp/health/ping

# 3. 检查详细健康状态
curl http://localhost:8080/api/admin/health
curl http://localhost:8081/api/miniapp/health
```

### 五、配置Nginx（可选）

```bash
# 1. 复制Nginx配置
sudo cp nginx.conf /etc/nginx/sites-available/huakang

# 2. 修改域名
sudo nano /etc/nginx/sites-available/huakang
# 将 admin.yourdomain.com 改为你的域名
# 将 api.yourdomain.com 改为你的域名

# 3. 创建软链接
sudo ln -s /etc/nginx/sites-available/huakang /etc/nginx/sites-enabled/

# 4. 申请SSL证书（使用Let's Encrypt）
sudo certbot --nginx -d admin.yourdomain.com -d api.yourdomain.com

# 5. 测试配置
sudo nginx -t

# 6. 重载Nginx
sudo systemctl reload nginx
```

### 六、设置定时备份

```bash
# 1. 赋予执行权限
chmod +x backup-mysql.sh

# 2. 测试备份脚本
./backup-mysql.sh

# 3. 添加到crontab
crontab -e

# 4. 添加以下行（每天凌晨2点备份）
0 2 * * * /opt/huakang/backup-mysql.sh
```

---

## 📋 部署检查清单

### 部署前检查

- [ ] 已生成强JWT密钥（64位）
- [ ] 已生成Redis密码（32位）
- [ ] 已生成数据库密码（24位）
- [ ] 已创建并配置 `.env.prod` 文件
- [ ] 已配置阿里云OSS（AccessKey、Bucket等）
- [ ] 已配置微信小程序（AppID、Secret）
- [ ] 服务器满足最低配置要求
- [ ] 已安装Docker和Docker Compose

### 部署中检查

- [ ] Docker镜像构建成功
- [ ] MySQL容器启动成功
- [ ] Redis容器启动成功
- [ ] Admin服务容器启动成功
- [ ] Miniapp服务容器启动成功
- [ ] 数据库初始化完成

### 部署后检查

- [ ] Admin健康检查接口返回正常
- [ ] Miniapp健康检查接口返回正常
- [ ] 数据库连接正常
- [ ] Redis连接正常
- [ ] 登录功能正常
- [ ] 文件上传功能正常
- [ ] API接口响应正常
- [ ] 日志输出正常

### 安全检查

- [ ] JWT密钥已设置为强随机值
- [ ] Redis已设置密码
- [ ] 数据库已设置强密码
- [ ] 生产环境Knife4j已关闭
- [ ] 生产环境MyBatis SQL日志已关闭
- [ ] Nginx已配置SSL证书
- [ ] 防火墙已配置（仅开放80、443端口）

### 性能检查

- [ ] 数据库连接池配置合理
- [ ] Redis缓存正常工作
- [ ] 日志文件轮转配置正常
- [ ] 备份脚本配置正常

---

## 🔧 常用命令

### Docker管理

```bash
# 查看所有容器状态
docker-compose -f docker-compose.prod.yml ps

# 查看实时日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.prod.yml logs -f admin
docker-compose -f docker-compose.prod.yml logs -f miniapp

# 重启服务
docker-compose -f docker-compose.prod.yml restart admin
docker-compose -f docker-compose.prod.yml restart miniapp

# 停止所有服务
docker-compose -f docker-compose.prod.yml down

# 停止并删除数据卷（危险操作！）
docker-compose -f docker-compose.prod.yml down -v
```

### 数据库管理

```bash
# 进入MySQL容器
docker exec -it hk-mysql-prod mysql -uroot -p

# 备份数据库
./backup-mysql.sh

# 恢复数据库
gunzip < backup.sql.gz | docker exec -i hk-mysql-prod mysql -uroot -p hk_electronics
```

### 日志管理

```bash
# 查看Admin日志
docker exec hk-admin-prod tail -f /app/logs/huakang-admin-prod.log

# 查看Miniapp日志
docker exec hk-miniapp-prod tail -f /app/logs/huakang-miniapp-prod.log

# 查看Nginx日志
sudo tail -f /var/log/nginx/admin-access.log
sudo tail -f /var/log/nginx/miniapp-access.log
```

### 监控命令

```bash
# 查看容器资源使用
docker stats

# 查看磁盘使用
df -h

# 查看内存使用
free -h

# 查看网络连接
netstat -tunlp | grep -E '8080|8081|3306|6379'
```

---

## 🐛 故障排查

### 服务无法启动

```bash
# 1. 查看容器日志
docker-compose -f docker-compose.prod.yml logs admin

# 2. 检查环境变量
docker-compose -f docker-compose.prod.yml config

# 3. 检查端口占用
netstat -tunlp | grep 8080
```

### 数据库连接失败

```bash
# 1. 检查MySQL容器状态
docker-compose -f docker-compose.prod.yml ps mysql

# 2. 测试数据库连接
docker exec hk-mysql-prod mysql -uhk_prod -p -e "SELECT 1"

# 3. 查看MySQL日志
docker-compose -f docker-compose.prod.yml logs mysql
```

### Redis连接失败

```bash
# 1. 检查Redis容器状态
docker-compose -f docker-compose.prod.yml ps redis

# 2. 测试Redis连接
docker exec hk-redis-prod redis-cli -a <密码> ping

# 3. 查看Redis日志
docker-compose -f docker-compose.prod.yml logs redis
```

### 内存不足

```bash
# 1. 查看内存使用
free -h

# 2. 清理Docker缓存
docker system prune -a

# 3. 调整JVM内存（修改docker-compose.prod.yml）
JVM_OPTS: "-Xms256m -Xmx512m"
```

---

## 📊 性能优化建议

### 1. 数据库优化

- 定期执行 `ANALYZE TABLE` 更新统计信息
- 监控慢查询日志
- 根据实际情况调整 `innodb_buffer_pool_size`

### 2. Redis优化

- 监控内存使用，避免超过 `maxmemory`
- 定期检查持久化文件大小
- 根据实际情况调整 `maxmemory-policy`

### 3. 应用优化

- 监控JVM内存使用
- 定期清理日志文件
- 使用CDN加速静态资源

### 4. 服务器优化

- 配置防火墙规则
- 启用系统监控（如Prometheus + Grafana）
- 配置日志收集（如ELK Stack）

---

## 📞 技术支持

如遇到问题，请查看：

1. **日志文件**：`/app/logs/` 目录
2. **健康检查**：`/api/admin/health` 接口
3. **检查报告**：`上线前全局检查报告.md`
4. **部署计划**：`服务器部署计划.md`

---

**部署完成后，请务必修改所有默认密码和密钥！**
