# 项目解耦说明

## 📋 解耦目标

将 `huakang-miniapp`（小程序端）和 `huakang-admin`（后台管理端）完全解耦，使它们可以独立运行、独立部署、独立维护。

## 🔄 解耦方案

### 方案一：miniapp独立项目（推荐）

将 `huakang-miniapp` 模块移到项目根目录，作为独立项目：

```
电器项目-后端/
├── backend/                    # 后台管理项目（admin）
│   ├── huakang-common/
│   ├── huakang-mapper/
│   ├── huakang-service/
│   ├── huakang-admin/
│   └── pom.xml                 # admin项目的父pom
│
└── miniapp-backend/            # 小程序项目（miniapp，独立）
    ├── huakang-common/          # 共享模块（可选，可复制或依赖）
    ├── huakang-mapper/          # 共享模块（可选，可复制或依赖）
    ├── huakang-service/        # 共享模块（可选，可复制或依赖）
    ├── huakang-miniapp/
    └── pom.xml                 # miniapp项目的父pom
```

### 方案二：miniapp保留但独立构建（当前采用）

保持 `huakang-miniapp` 在 `backend/` 目录下，但从父pom中移除，使其可以独立构建：

```
backend/
├── huakang-common/             # 共享模块
├── huakang-mapper/             # 共享模块
├── huakang-service/            # 共享模块
├── huakang-admin/              # admin项目（依赖共享模块）
├── huakang-miniapp/            # miniapp项目（独立，不依赖父pom）
└── pom.xml                      # admin项目的父pom（不包含miniapp）
```

## ✅ 已执行的解耦操作

### 1. 从父pom中移除miniapp模块

**文件**：`backend/pom.xml`

**变更**：
- 从 `<modules>` 中移除 `<module>huakang-miniapp</module>`
- 添加注释说明miniapp已解耦

**影响**：
- `mvn clean install` 在backend目录下不再构建miniapp
- miniapp需要独立构建和运行

### 2. miniapp独立构建配置

**文件**：`backend/huakang-miniapp/pom.xml`

**需要修改**：
- 将parent改为Spring Boot的parent，或创建独立的父pom
- 确保miniapp可以独立编译和运行

## 📝 后续操作建议

### 选项A：完全移除miniapp（如果不需要）

如果当前不需要miniapp功能，可以：

1. 删除 `backend/huakang-miniapp/` 目录
2. 更新文档，说明miniapp是独立项目

### 选项B：保留miniapp但独立构建

如果需要保留miniapp，但独立构建：

1. 修改 `backend/huakang-miniapp/pom.xml`，使其不依赖父pom
2. 在miniapp目录下独立运行 `mvn clean install`
3. 独立启动：`java -jar huakang-miniapp/target/huakang-miniapp-1.0.0.jar`

### 选项C：将miniapp移到独立项目

如果需要完全分离：

1. 创建 `miniapp-backend/` 目录
2. 移动 `backend/huakang-miniapp/` 到 `miniapp-backend/huakang-miniapp/`
3. 复制或共享 `huakang-common`、`huakang-mapper`、`huakang-service` 模块
4. 创建独立的父pom

## 🎯 当前状态

- ✅ **admin项目**：可以独立构建和运行
- ⚠️ **miniapp项目**：需要修改pom.xml才能独立构建

## 📌 注意事项

1. **共享模块**：`huakang-common`、`huakang-mapper`、`huakang-service` 仍然被admin和miniapp共享
   - 如果miniapp需要独立，可以：
     - 复制这些模块到miniapp项目
     - 或通过Maven依赖引入（需要发布到Maven仓库）
     - 或使用Git子模块

2. **数据库和Redis**：两个项目仍然共享同一个数据库和Redis
   - 这是正常的，因为它们操作的是同一套业务数据
   - 可以通过不同的database索引区分（admin用db0，miniapp用db1）

3. **配置文件**：两个项目有独立的 `application.yml`
   - admin：`backend/huakang-admin/src/main/resources/application.yml`
   - miniapp：`backend/huakang-miniapp/src/main/resources/application.yml`

## 🚀 独立运行方式

### Admin项目

```bash
cd backend
mvn clean install
cd huakang-admin
mvn spring-boot:run
# 或
java -jar target/huakang-admin-1.0.0.jar
```

### Miniapp项目（需要先修改pom.xml）

```bash
cd backend/huakang-miniapp
# 需要先修改pom.xml使其独立
mvn clean install
mvn spring-boot:run
# 或
java -jar target/huakang-miniapp-1.0.0.jar
```
