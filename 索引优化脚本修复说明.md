# 索引优化脚本修复说明

## ❌ 问题描述

执行 `index_optimization.sql` 时出现错误：

```
[42000][1072] Key column 'last_login_at' doesn't exist in table
```

**原因：** `members` 表中没有 `last_login_at` 字段，该字段只在 `admins` 和 `staffs` 表中存在。

---

## ✅ 已修复

已从 `index_optimization.sql` 中删除了对 `members.last_login_at` 的索引创建语句。

**修复内容：**
- ❌ 删除了：`ALTER TABLE members ADD KEY idx_last_login_at (last_login_at);`
- ✅ 保留了：`ALTER TABLE members ADD KEY idx_deleted_created (is_deleted, created_at DESC);`

---

## 🔄 重新执行

### 方法一：在Navicat中执行

1. 打开 `index_optimization.sql`
2. 点击"运行"执行脚本
3. 应该不会再报错

### 方法二：使用命令行

```bash
# 方式1：使用docker compose
docker compose exec -T mysql mysql -uroot -proot hk_electronics < index_optimization.sql

# 方式2：直接执行
mysql -h localhost -P 3307 -u root -proot hk_electronics < index_optimization.sql
```

---

## 📊 验证索引创建

执行以下SQL验证索引是否创建成功：

```sql
-- 查看members表索引
SHOW INDEX FROM members;

-- 查看products表索引
SHOW INDEX FROM products;

-- 查看points_records表索引
SHOW INDEX FROM points_records;

-- 查看staffs表索引
SHOW INDEX FROM staffs;

-- 查看admins表索引
SHOW INDEX FROM admins;
```

---

## 📝 索引说明

### members表索引
- `idx_deleted_created` - 软删除+创建时间复合索引（用于列表查询）

### products表索引
- `idx_type_status_sort` - 全量商品列表排序优化
- `idx_category_type_status` - 分类查询优化
- `idx_type_status_points` - 积分商品按价格排序

### points_records表索引
- `idx_member_type_created` - 会员+变动类型+时间复合索引
- `idx_created_type` - 时间范围+变动类型统计
- `idx_points` - 积分字段索引（用于统计查询）

### staffs表索引
- `idx_status_deleted` - 状态+软删除+创建时间复合索引

### admins表索引
- `idx_deleted_created` - 软删除+创建时间复合索引

---

## ⚠️ 注意事项

1. **索引已存在**：如果索引已存在，执行 `ALTER TABLE ADD KEY` 会报错，可以忽略或先删除再创建
2. **性能影响**：索引会占用存储空间，并略微影响写入性能，但能大幅提升查询性能
3. **定期维护**：建议定期执行 `ANALYZE TABLE` 更新索引统计信息

---

**脚本已修复，可以重新执行！** 🎉
