# 环境配置说明

## 环境概览

项目支持三个环境配置：

1. **开发环境 (dev)** - 本地开发使用
2. **测试环境 (test)** - 测试服务器使用
3. **生产环境 (prod)** - 生产服务器使用

## 切换环境

### 方式1：修改 application.yml

在 `application.yml` 文件顶部修改：

```yaml
spring:
  profiles:
    active: dev  # 改为 test 或 prod
```

### 方式2：启动参数

```bash
java -jar app.jar --spring.profiles.active=test
```

### 方式3：环境变量

```bash
export SPRING_PROFILES_ACTIVE=prod
```

## 环境配置详情

### 开发环境 (dev)

**数据库配置：**
- 主机：`localhost:3307`
- 数据库：`hk_electronics`
- 用户名：`root`
- 密码：`root`
- 连接池：最小5，最大20

**Redis配置：**
- 主机：`localhost:6379`
- 数据库：`0`
- 无密码

**日志级别：** DEBUG

**API文档：** 启用

---

### 测试环境 (test)

**数据库配置（支持环境变量）：**
- 主机：`${TEST_DB_HOST:localhost}:${TEST_DB_PORT:3307}`
- 数据库：`${TEST_DB_NAME:hk_electronics_test}`
- 用户名：`${TEST_DB_USERNAME:root}`
- 密码：`${TEST_DB_PASSWORD:root}`
- 连接池：最小3，最大10

**Redis配置（支持环境变量）：**
- 主机：`${TEST_REDIS_HOST:localhost}`
- 端口：`${TEST_REDIS_PORT:6379}`
- 密码：`${TEST_REDIS_PASSWORD:}`
- 数据库：`1`（使用不同的数据库避免与开发环境冲突）

**日志级别：** DEBUG

**API文档：** 启用

**环境变量示例：**
```bash
TEST_DB_HOST=test-db.example.com
TEST_DB_PORT=3306
TEST_DB_NAME=hk_electronics_test
TEST_DB_USERNAME=test_user
TEST_DB_PASSWORD=test_password
TEST_REDIS_HOST=test-redis.example.com
TEST_REDIS_PORT=6379
TEST_REDIS_PASSWORD=test_redis_password
TEST_SERVER_PORT=8080
```

---

### 生产环境 (prod)

**数据库配置（必须使用环境变量）：**
- 主机：`${DB_HOST:localhost}:${DB_PORT:3306}`
- 数据库：`${DB_NAME:hk_electronics}`
- 用户名：`${DB_USERNAME:root}`
- 密码：`${DB_PASSWORD:root}`
- 连接池：最小10，最大50
- SSL：启用

**Redis配置（必须使用环境变量）：**
- 主机：`${REDIS_HOST:localhost}`
- 端口：`${REDIS_PORT:6379}`
- 密码：`${REDIS_PASSWORD:}`
- 数据库：`0`
- 连接池：最大20

**日志级别：** INFO

**API文档：** 禁用（安全考虑）

**JWT密钥：** 使用环境变量 `${JWT_SECRET}`

**环境变量示例：**
```bash
DB_HOST=prod-db.example.com
DB_PORT=3306
DB_NAME=hk_electronics
DB_USERNAME=prod_user
DB_PASSWORD=prod_password
REDIS_HOST=prod-redis.example.com
REDIS_PORT=6379
REDIS_PASSWORD=prod_redis_password
SERVER_PORT=8080
JWT_SECRET=your-super-secret-key-here
```

## 环境变量配置建议

### 开发环境
- 直接使用配置文件中的默认值
- 无需设置环境变量

### 测试环境
- 建议使用环境变量配置
- 使用独立的测试数据库和Redis
- Redis使用 database 1 避免冲突

### 生产环境
- **必须使用环境变量配置**
- 不要在生产环境配置文件中硬编码敏感信息
- 使用强密码和安全的JWT密钥
- 启用SSL连接

## 注意事项

1. **数据库名称：**
   - 开发环境：`hk_electronics`
   - 测试环境：`hk_electronics_test`（建议）
   - 生产环境：`hk_electronics`

2. **Redis数据库：**
   - 开发环境：`database: 0`
   - 测试环境：`database: 1`（避免冲突）
   - 生产环境：`database: 0`

3. **日志文件：**
   - 开发环境：`logs/huakang-admin.log`
   - 测试环境：`logs/huakang-admin-test.log`
   - 生产环境：`logs/huakang-admin-prod.log`（带日志轮转）

4. **API文档：**
   - 开发/测试环境：启用
   - 生产环境：禁用（安全考虑）

5. **连接池配置：**
   - 开发环境：较小连接池（5-20）
   - 测试环境：中等连接池（3-10）
   - 生产环境：较大连接池（10-50）

## 快速切换示例

### 本地开发
```yaml
spring:
  profiles:
    active: dev
```

### 测试服务器
```yaml
spring:
  profiles:
    active: test
```
并设置相应的环境变量。

### 生产服务器
```yaml
spring:
  profiles:
    active: prod
```
**必须**设置所有环境变量，不要使用默认值。
