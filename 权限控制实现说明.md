# 权限控制实现说明

## 🎯 实现方式

### 1. 权限注解（@RequireRole）

在Controller类或方法上使用 `@RequireRole` 注解来指定允许的角色：

```java
@RequireRole("admin")              // 仅管理员
@RequireRole({"admin", "staff"})   // 管理员和店员都可以
```

### 2. 拦截器（RoleCheckInterceptor）

自动拦截所有请求，检查用户角色是否符合要求。

### 3. 配置（WebMvcConfig）

注册拦截器，排除不需要权限检查的接口（登录、文档等）。

---

## 📋 各模块权限配置

### 数据大屏模块
```java
@RequireRole("admin")  // 仅管理员
public class DashboardController { ... }
```

### 会员管理模块
```java
@RequireRole({"admin", "staff"})  // 管理员和店员都可以
public class MemberController { ... }
```

### 积分记录模块
```java
@RequireRole({"admin", "staff"})  // 管理员和店员都可以
public class PointsRecordController { ... }
```
**注意**：虽然接口权限相同，但数据权限不同（店员只能看自己的记录）

### 店员管理模块
```java
@RequireRole("admin")  // 仅管理员
public class StaffController { ... }
```

### 导出功能模块
```java
@RequireRole("admin")  // 仅管理员
public class ExportController { ... }
```

---

## 🔍 权限验证流程

```
1. 用户请求接口
   ↓
2. RoleCheckInterceptor 拦截请求
   ↓
3. 检查接口是否有 @RequireRole 注解
   ↓
4. 从JWT Token中获取用户角色
   ↓
5. 验证角色是否在允许的角色列表中
   ↓
6. 通过：继续执行
   失败：抛出 BusinessException("无权限访问")
```

---

## ⚠️ 注意事项

1. **Token必须包含角色信息**：JWT Token中必须包含 `role` 字段（admin/staff）
2. **拦截器排除路径**：登录、文档等接口已排除，不会进行权限检查
3. **数据权限**：接口权限控制访问，数据权限在Service层控制数据范围

---

## 🧪 测试权限控制

### 测试管理员权限

1. 使用管理员账号登录
2. 访问所有接口应该都能成功

### 测试店员权限

1. 使用店员账号登录
2. **应该成功**：
   - ✅ 会员管理接口
   - ✅ 积分记录查询接口
3. **应该失败（403或业务异常）**：
   - ❌ 数据大屏接口
   - ❌ 店员管理接口
   - ❌ 导出功能接口

---

## 🔧 如何添加新的权限控制

### 场景1：新接口仅管理员可访问

```java
@RequireRole("admin")
@GetMapping("/new-endpoint")
public Result<?> newEndpoint() {
    // ...
}
```

### 场景2：新接口管理员和店员都可访问

```java
@RequireRole({"admin", "staff"})
@GetMapping("/new-endpoint")
public Result<?> newEndpoint() {
    // ...
}
```

### 场景3：类级别权限，方法级别覆盖

```java
@RequireRole("admin")  // 类级别：默认仅管理员
public class MyController {
    
    @RequireRole({"admin", "staff"})  // 方法级别：覆盖类级别
    @GetMapping("/public-method")
    public Result<?> publicMethod() {
        // ...
    }
}
```
