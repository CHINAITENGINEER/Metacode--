# 快速添加SQL脚本到GitHub

## 🚀 快速命令（复制粘贴即可）

在项目根目录（`D:\电器项目-后端`）打开命令行，执行：

```bash
# 添加所有SQL文件
git add *.sql

# 提交
git commit -m "添加数据库初始化脚本和索引优化脚本"

# 推送
git push origin main
```

---

## 📋 需要添加的文件

- ✅ `database_init_enterprise.sql` - 企业级数据库初始化脚本
- ✅ `database_init.sql` - 基础数据库初始化脚本  
- ✅ `index_optimization.sql` - 索引优化脚本

---

## ✅ 完成！

执行完上述命令后，SQL文件就会出现在GitHub仓库中了！
